# Issue Council Debate — Persona 08 (Visual Storyteller)

> Author: jwogrady. Phase 4 — Debate. Positions argued from the visual layer domain.

---

## My own nominations: standing firm

### N08-1 — `spark doctor` demo GIF (P1)

I am holding P1. The GIF is the only visual proof-of-life for the CLI on the README.
The Quickstart section tells a user to run `spark doctor` — without a visual they have
no signal that the command does anything beyond exit zero. This is not a polish item.
It is the difference between a README that claims mechanical enforcement and one that
shows it. The capture instructions are in `08-visuals.md`; the asset takes one
terminal session to produce. No-brainer admission.

Persona 02 (Adopter) co-signed the gap from their quickstart angle
(`from-02-to-08.md`). No one has contested it.

**Position: ADMIT, P1. Non-negotiable for pre-launch.**

---

### N08-2 — Lifecycle Mermaid diagram (P1)

Also holding P1. The diagram is ready to paste — Mermaid source, alt text, and the
stage-to-skill caption that bridges the "Generate" vs. `/spark:build` naming gap are
all in `08-visuals.md` Asset 1. This is a zero-effort, paste-in visual that makes
the lifecycle scannable in under five seconds.

Persona 06 (Coach) independently confirmed the stage-name/command gap as real
(`from-06-to-08.md`, Issue 2). No one has contested this nomination.

**Position: ADMIT, P1. The diagram is ready; the only blocker is someone pasting it in.**

---

### N08-3 — ASCII component map (P2)

Holding P2. The diagram shows Spark's additive-by-design layering — a verified genuine
differentiator (`00-ground-truth.md` §Genuine differentiators). It was corrected during
Phase 2 to accurately separate `hooks/guard-bash.sh` from `scripts/hooks/commit-msg`
and `pre-commit`. It is ASCII, renders everywhere, carries a real architectural claim.

Not P1 because the lifecycle diagram is more urgently scannable; this one belongs in
the "How it fits together" section further down. But it must ship — a README that
asserts "additive by design" with no visual is leaving a verified differentiator in
prose-only form.

**Position: ADMIT, P2.**

---

### N08-4 — Social-preview OG image (P2)

Holding P2, not P1. I understand Persona 10 (Discoverer) wants social-preview
metadata configured at P2. We agree on priority. The asset concept is fully specified
in `08-visuals.md` Asset 5 (1280×640, deep navy, five-stage flow, `jwogrady/spark`
monospace, no AI attribution). It is a creation task — the author or a designer needs
to produce it. It is not a launch blocker for the README itself; it is a discoverability
lift for every share after launch. Co-signing Persona 10's social metadata nomination
from the visual-ownership angle.

**Position: ADMIT, P2. The concept is specified; creation is on the author.**

---

### N08-5 — Canonical skill layout spec (P2, blocked visual)

Holding P2. The skill directory anatomy tree (Asset 8) cannot be finalized until the
`CONTRIBUTING.md` vs. `write-a-skill/SKILL.md` layout conflict is resolved. I am not
nominating the diagram itself as a standalone issue — I am co-signing Persona 07's
N07-1 and Persona 06's N06-B as the blocking prerequisite for it. Once the spec is
canonical, the anatomy tree is a ten-line ASCII diagram. Until then, any diagram would
be wrong against one of the two conflicting sources.

**Position: Co-sign N07-1 and N06-B at P1. Once resolved, the anatomy diagram
ships as a P3 follow-on.**

---

## Responses to other personas' nominations

---

### N00-1, N01-2, N03-2, N04-1, N05-3, N09-3, N10-4, N11-1 — License mismatch (all claiming P1)

**CO-SIGN at P1 — and I am adding visual-layer weight.**

The license overclaim affects the visual layer directly: the README carries an MIT
badge (`![license](https://img.shields.io/badge/license-MIT-green)`) that is wrong the
moment anyone reads `LICENSE`. The badge is a visual element I own.

Eight separate personas have nominated this. The convergence is unprecedented. I fully
co-sign the P1 designation. The badge must come down or the LICENSE file must be
updated to MIT — no third option. My stake: the README's badge row is the first
trust-signal a visitor sees; a wrong badge undermines every other visual claim I
worked to make credible.

**ADMIT at P1. The badge is my responsibility to flag; I am flagging it.**

---

### N00-2, N01-3, N03-4, N06-F, N07-4, N09-4 — `list-skills` CLI undercount

**CO-SIGN at P3.**

My Phase 2 cross-eval from Persona 00 (`from-00-to-08.md`) already caught this: the
component map's CLI column previously omitted `list-skills` and `help`. I corrected the
ASCII diagram. The README prose and `docs/reference/cli.md` still need the fix.

It is P3, not P1 — `list-skills` is discoverable via `spark help`. The fix is one
line in the README CLI list and one table row in `docs/reference/cli.md`. Six personas
nominating the same P3 item is confirmation, not escalation.

**ADMIT at P3. But resist any attempt to inflate to P1 or P2 — the tool surface
is not broken, only underdocumented.**

---

### N00-3 — Hook file-layout accuracy in architecture diagram

**CO-SIGN at P2 — with a correction status note.**

This was the most critical accuracy finding in my Phase 2 cross-eval. Persona 00's
feedback is exactly right and was already RESOLVED in Phase 3: the corrected ASCII
component map in `08-visuals.md` Asset 2 separates `hooks/guard-bash.sh` from
`scripts/hooks/commit-msg` and `scripts/hooks/pre-commit` into two distinct cells,
matching the real directory layout.

**What the Council needs to know:** this issue is about ensuring the corrected diagram
actually lands in the README, not that the correction hasn't been made. It has been
made in the draft — the gap is the authoring task of shipping it. Track it at P2 as
part of the component map (N08-3) landing, not as a separate diagram-correction task.

**ADMIT at P2, merged with N08-3 in implementation.**

---

### N00-4 — `spark doctor` output format / no "OK" string

**CO-SIGN at P2, with a visual-specific implication.**

This directly affects my GIF nomination. If the Quickstart or any caption quotes
`spark doctor` output incorrectly ("OK", "16 skills / 19 agents"), and a user watches
the real GIF, the mismatch destroys trust in exactly the moment the GIF is supposed to
build it.

My capture instructions in `08-visuals.md` Asset 3 already say "do not ship a
staged/fake recording." But I am formally co-signing N00-4 because the prose captions
around the GIF must quote the real `Healthy — 0 errors, N warning(s)` string, not
any invented tally or "OK" shorthand.

**ADMIT at P2. The GIF itself will show real output; the surrounding prose must match.**

---

### N01-1 — Hero tagline revision not integrated into README

**CO-SIGN at P1.**

The hero tagline anchors every visual — it appears in the social-preview card concept
and in the lifecycle diagram placement rationale. If the README still shows the
pre-revision tagline, the visual layer I specify is pointing at a stale hook. The
Skimmer's Phase 3 revision is the right text; it must land.

**ADMIT at P1. I am also a downstream stakeholder: the social-preview image concept
in `08-visuals.md` Asset 5 uses the revised tagline and will be out of sync if this
is not resolved first.**

---

### N01-4, N02-2, N03-1, N11-4 — Marketplace install caveat missing from README

**CO-SIGN at P2.**

My install command block (Asset 4 in `08-visuals.md`) shows the marketplace slash
commands in a code fence. If the marketplace listing is not yet live, a reader who
copies that block hits a dead end. The caveat must be in the README install section,
not just in launch copy.

I note four personas rated this P1 or P2 with varying reasoning — they all converge
on the same fix: one qualifier line in the README install block pointing to the Git-URL
path. I would rate it P2 not P1 because the install path itself is not broken for
someone with a local clone; it is misleading for someone expecting a published listing.

**ADMIT at P2. The install code fence I specify must carry the caveat inline.**

---

### N02-4 — `/spark:plan` GitHub issue creation not shipped in v0.2

**CONTEST the P1 designation, but ADMIT the issue.**

Persona 02 nominates this at P1 ("breaks the complete Ideate → Plan → Ship happy
path"). From a visual-layer standpoint, the lifecycle flow diagram is accurate and does
not assert that `/spark:plan` creates live GitHub issues — it shows the stage name
`Plan` with a skill reference. The diagram itself is not broken.

The issue is real: the quickstart prose overclaims what `/spark:plan` does in v0.2.
But calling it a P1 that "breaks the happy path" overstates the visual impact. The
lifecycle diagram and the command block do not make this claim. The overclaim lives in
the quickstart prose, not in any visual. Downgrade to P2.

**ADMIT at P2. The visual layer does not need to be corrected for this item, but the
quickstart prose does.**

---

### N04-2 — CI workflow missing

**CONTEST — out of scope for this docit run.**

The Evaluator nominates CI as a P2 gap. This is a real engineering gap, not a
documentation gap. The docit crew's mandate is to improve public-facing docs; CI
infrastructure is outside our scope. The honest-hype angle is valid (no CI badge means
no CI badge should appear in the README — which Persona 04's own note already handles
correctly by marking the CI badge "NOT READY — omit"). The documentation outcome is
already handled: don't show a CI badge. Creating a CI workflow is a code/infra change
for a separate PR, not a docit issue.

**CONTEST. ADMIT as out of scope for docit — redirect to a separate engineering issue.**

---

### N05-1, N06-A — `docs/explanation/enforcement-model.md` missing (P1)

**CO-SIGN at P1 from a visual angle.**

The enforcement model is the strongest verifiable claim in the codebase — mechanical
hooks, real file paths, real behavior. It is the honest-hype backbone for every visual
I produce. If there is no explanation doc for why mechanical enforcement was chosen,
then when the philosophy ships and links to this concept, readers hit a dead end.

My stake is indirect but real: the architecture component map (Asset 2) shows the
enforcement layer. If a reader wants to understand *why* that layer works the way it
does, they must have somewhere to go. `enforcement-model.md` is that destination.

**ADMIT at P1. This doc gates the philosophy and indirectly gates the architecture
diagram's depth.**

---

### N06-C — `docs/tutorials/set-up-a-new-project.md` (Tutorial 1)

**CO-SIGN at P1, with a visual-layer dependency note.**

The setup tutorial is where the `spark doctor` GIF would most naturally live as its
first embedded visual. Right now there is no tutorial home for it other than the
Quickstart section of the README. Once Tutorial 1 exists, the GIF belongs there
(linked from the README) rather than embedded inline. If Tutorial 1 is not created
before the README ships, the GIF placement decision becomes entangled.

**ADMIT at P1. Also: the setup tutorial is the natural home for the GIF; should be
sequenced before finalizing GIF placement.**

---

### N06-D — `docs/how-to/commit.md` and fix mode bleed in `ship.md`

**CO-SIGN at P1.**

The stage-to-skill caption in the lifecycle diagram (Asset 1) distinguishes Stage 5a
(`/spark:commit`) from Stage 5b (`/spark:ship`). If the how-to tree blurs these two
stages in one doc, the visual claim the diagram makes — that commit and ship are
distinct skills — is contradicted by the documentation structure. This is a rare case
where a docs mode violation directly conflicts with a visual I produce.

**ADMIT at P1. The lifecycle diagram's caption is more honest than the docs structure
it currently points at.**

---

### N09-1, N04-3, N11-2 — `codify` missing from README and CHANGELOG

**CO-SIGN at P1 (README), P1 (CHANGELOG).**

This has a visual dimension: the component map and the launch copy both promote
`codify`. If the README does not mention `codify`, the architecture diagram becomes
confusing — it shows a multi-persona authorship layer that the README text never names.
A diagram cannot exist in isolation from the prose that explains it.

**ADMIT at P1. The README must name `codify` before the architecture diagram ships.**

---

### N10-1 — GitHub topics not configured

**CO-SIGN at P1.**

Topics are metadata, not visuals, but they directly affect who finds the repo and
therefore who encounters my visuals. Zero cost, instant lift. The topic list Persona 10
drafted matches verified capabilities. Nothing to contest.

**ADMIT at P1.**

---

### N10-2 — Awesome-list submissions

**CO-SIGN at P1 — but with a sequencing note.**

Awesome-list submissions should not go out while the license is TBD and `codify` is
absent from the README. The list reviewers will look at the repo; what they find must
match what the submission claims. Sequence: resolve N00-1 (license) and N11-2 (codify
in README) first, then submit.

**ADMIT at P1, but flag as sequencing dependency on license resolution and README
completeness.**

---

### N11-3 — `docs/launch-copy.md` does not exist

**NEUTRAL on this one.**

The Amplifier's persona spec says the Editor assembles launch copy into
`docs/launch-copy.md`. That is correct per the docit crew design. Whether this becomes
a tracked GitHub issue or simply an Editor task is a meta-process question, not a
README or visual gap. I will not contest it, but I will not push it higher than P2.

**ADMIT at P2. Does not affect the visual layer.**

---

## Summary of strongest positions

| Issue | My position | Rationale |
|---|---|---|
| License badge (N00-1 et al.) | CO-SIGN P1 | Badge is mine to correct; eight co-nominators confirm it |
| `spark doctor` GIF (N08-1) | OWN P1 | Only visual proof-of-life; Quickstart incomplete without it |
| Lifecycle Mermaid diagram (N08-2) | OWN P1 | Ready to paste; bridges stage/command naming gap |
| Hero tagline not shipped (N01-1) | CO-SIGN P1 | Diagram placement and social-preview depend on correct tagline |
| Enforcement model doc (N05-1/N06-A) | CO-SIGN P1 | Architecture diagram's explanation destination |
| `commit.md` / `ship.md` mode bleed (N06-D) | CO-SIGN P1 | Diagram caption asserts what docs do not confirm |
| Tutorial 1 setup path (N06-C) | CO-SIGN P1 | Natural home for GIF; must exist before final placement |
| `codify` in README (N11-2) | CO-SIGN P1 | Architecture diagram mentions it; README must too |
| Component map corrected (N08-3) | OWN P2 | Additive-by-design differentiator deserves a visual |
| Social-preview OG image (N08-4/N10-3) | OWN P2 | Concept specified; creation task for author |
| Marketplace caveat (N01-4 et al.) | CO-SIGN P2 | Install code fence I specify must carry the qualifier |
| `list-skills` undercount | CO-SIGN P3 | Real accuracy gap; resist inflation to P1/P2 |
| CI workflow missing (N04-2) | CONTEST | Out of scope for docit; redirect to engineering issue |
| `/spark:plan` P1 designation (N02-4) | CONTEST downgrade to P2 | Visual layer not broken; prose overclaim only |
