# Issue Council — Debate from 00 (Cartographer)

> Phase 4 — Debate. Author/credit string is always `jwogrady`.
> As Cartographer I hold the honest-hype veto. Every position below traces to
> `00-ground-truth.md` or a fact I re-verified this phase (see "Verified this
> phase"). Co-sign = add weight. Contest = duplicate / out of scope / low value /
> unsafe. Counter = reframe / merge / split. VETO = must never ship as written.

## Verified this phase (re-ran the commands)

- `.claude-plugin/plugin.json` → `"version": "0.2.0"`. Any "v0.3.0 current" claim is false.
- `CHANGELOG.md` `[Unreleased]` (line 9) contains **no** `codify` and **no** `review` entry. Both nominations (04-3, 09-1, 11-2) are factually grounded.
- `docs/explanation/` holds exactly `scope-and-upstream.md`, `sdlc-doctrine.md`, `why-a-plugin.md`. No `philosophy.md`, no `enforcement-model.md`, no `authorship-crews.md`.
- Neither `docs/PHILOSOPHY.md` nor `docs/explanation/philosophy.md` exists.
- `docs/how-to/` has no `commit.md`; `ship.md` is present (06-D grounded).
- `.github/workflows/` does not exist (04-2 grounded).
- Skill-layout conflict is real: `CONTRIBUTING.md` lines 49-50 (`references/`, `agents/`) vs `skills/write-a-skill/SKILL.md` lines 31-33 (`REFERENCE.md`, `EXAMPLES.md`, `scripts/`). 05-5, 06-B, 07-1, 08-5 all grounded.

---

## The license cluster — CO-SIGN, and I merge it into ONE issue

Six personas nominated the same defect: N00-1, 01-2, 03-2, 04-1, 05-3, 09-3,
10-4, 11-1. This is the single highest-trust risk in the repo and I co-sign every
one of them with maximum weight. But eight separate issues for one binary
decision is council noise. **Counter: merge into a single P1 issue — "Resolve the
MIT license overclaim"** — with the downstream surfaces (README badge, manifest,
CHANGELOG note, launch-copy caveat) listed as a checklist inside it, not as
independent issues. The fix is one decision (adopt MIT text, or drop the MIT
claim) plus the edits that cascade from it.

**Cartographer's standing veto stays armed:** until `LICENSE` contains a real
SPDX grant, no shipped surface may assert MIT as adopted. The badge and the
`plugin.json` `"license": "MIT"` field are both overclaims today.

## The `list-skills` undercount — CO-SIGN, merge, low priority

N00-2, 01-3, 03-4, 05-7, 06-F, 07-4, 09-4 are all the same one-line accuracy fix
across three surfaces (README "What's in the box", `docs/reference/cli.md`, the
changelog). **Counter: merge into one P3 issue** that fixes all three surfaces at
once. Verified-real (`cmd_list_skills` in `bin/spark`). Cheap, correct, but not a
launch blocker — resist any attempt to inflate it to P1.

## `codify`/`review` missing from CHANGELOG — CO-SIGN (P1)

04-3 (codify + review) and 09-1 / 11-2 (codify) are grounded: I re-verified the
`[Unreleased]` block has neither. **Co-sign, and counter to widen 09-1/11-2 to
include `review`** as 04-3 correctly does — both shipped in the unreleased window
and both are absent. One issue: "Record `codify` and `review` in CHANGELOG
`[Unreleased]`." This is honest-hype hygiene: shipped features the front-door
record omits.

## The doc-tree gaps (philosophy / enforcement-model / authorship-crews) — CO-SIGN with a path-conflict flag

05-1, 05-2, 05-4, 06-A, 06-E, 06-G nominate writing `philosophy.md`,
`enforcement-model.md`, `authorship-crews.md` and wiring them into nav. I co-sign
all three as grounded gaps (none exist; the differentiators they document are
real). **But I flag a contradiction the council must resolve before any of these
ship: the target path is itself contested.** 05-1 and 03-3 target
`docs/explanation/philosophy.md`; 06-A/06-E/06-G repeatedly write
`docs/PHILOSOPHY.md` (repo root-of-docs). These are different locations. Ship one,
not both, or you create the exact orphaned-doc problem 06-G is trying to prevent.
My ruling as Cartographer: **`docs/explanation/philosophy.md`** — it is an
explanation-mode doc and the rest of the tree is already under `docs/explanation/`;
`docs/PHILOSOPHY.md` would break the Diátaxis grouping these same personas argue
for. Counter accepted only with that path pinned.

## The marketplace-install caveat — CO-SIGN, merge

01-4, 02-2, 03-1, 11-4 all nominate adding the "marketplace listing is a v0.2
open item; use local clone / Git URL" caveat to the README install block.
Grounded in ROADMAP. **Counter: merge into one P1 issue.** The README is the only
public surface that still asserts the marketplace path silently; the other
surfaces already carry the caveat per their drafts.

## Skill-layout spec conflict — CO-SIGN (P1, it's a real blocker)

05-5, 06-B, 07-1, 07-2, 08-5 converge on the `CONTRIBUTING.md` vs
`write-a-skill/SKILL.md` contradiction, which I re-verified. **Co-sign at P1** —
it blocks the `write-a-skill` how-to (07-2), the skill-anatomy diagram (08-5), and
any `doctor` layout validation. Counter: track the *decision* (pick canonical
layout + record as an ADR, fitting the existing ADR-0001..0003 series) as the
blocking issue; 07-2 (write the how-to) and 08-5 (draw the diagram) are
*downstream* and should be marked blocked-by it, not voted as peers.

---

## CONTESTED — scope / value

- **02-6 (`spark doctor` machine-readable output contract for CI):** Contest as
  **out of scope / speculative**. There is no evidence a stable output contract was
  ever promised; inventing one is a feature request dressed as a doc gap. Fold the
  legitimate kernel (document the *real* output strings) into N00-4. Reject the
  "exit codes + output schema for scripting" framing — that is roadmap invention.

- **04-2 (add CI workflow):** Contest as **out of scope for a docit run**. It is a
  real gap and a fair P2/P3 *backlog* item, but docit ships docs, not CI pipelines,
  and CLAUDE.md forbids touching `.github/workflows/` without full-pipeline
  understanding. Keep it as a noted roadmap issue; do not let it block the docs.

- **04-4 (breaking-change policy doc):** Low value for this run. A one-line
  "treat any v0.x as potentially breaking" note is fine; a dedicated policy doc is
  premature at v0.2.0. Downgrade to P3, fold into the README/trust copy.

- **09-2 (git-hook upgrade path for custom hooks):** Co-sign as **P3 doc note
  only**. `install-git-hooks` already refuses to overwrite a non-Spark hook
  (verified, ground truth line 55), so the "silent failure" framing overstates it.
  A short coexistence note is warranted; a migration system is not.

- **10-1 / 10-2 / 10-3 (GitHub topics / awesome-list submissions / social
  metadata) and 08-4 (og-image):** Contest as **out of docit's shipping scope**.
  These are author actions in GitHub settings and external directories, not repo
  artifacts docit writes. Legitimate launch tasks — capture them in 11-3's
  `docs/launch-copy.md`, do not seat them as code/doc issues. Also: **10-2
  awesome-list submission is contingent on the license veto** — do not submit
  Spark to any list while it advertises an unadopted MIT license. I tie 10-2 to
  the license merge as a hard prerequisite.

- **10-5 (crew discoverability landing pages):** Contest as **duplicate** of 06-E
  (`authorship-crews.md`). Merge; do not create a parallel "landing page" track.

- **08-1 (`spark doctor` demo GIF), 08-2 (Mermaid lifecycle diagram), 08-3
  (component map):** Co-sign 08-2 and 08-3 as paste-ready, verified, in-scope
  README additions (P2). **08-1 (GIF)** I co-sign only with a guard: the GIF
  caption/alt text must quote the *real* doctor output (`✓ <name>` per item, final
  `Healthy — 0 errors, N warning(s)`), not a fabricated "OK" or "16/19 tally" —
  see my N00-4 veto.

---

## VETOES (honest-hype — these never ship as written)

1. **"v0.3.0" as the current/released version.** Manifest is `0.2.0`. Any
   CHANGELOG or copy stating 0.3.0 as shipped is VETOED; allowed only as
   `[Unreleased]`. (Re-verified this phase.)

2. **"codify coordinates through `.docit-notes/` (shared with docit)."** False —
   codify uses `.codify-notes/`. HARD VETO (N00-6). I did not see another persona
   repeat this in nominations, but it remains struck on sight if it appears in any
   assembled draft.

3. **MIT advertised as the adopted license** anywhere (badge, manifest, README,
   launch copy, awesome-list entry) while `LICENSE` reads "License TBD." VETOED
   until the license merge resolves. This also gates 10-2.

4. **`spark doctor` quoted as printing "OK" or a "16 skills / 19 agents" tally.**
   VETOED. Replace with the real `✓ <name>` lines + `Healthy — 0 errors,
   N warning(s)`. Applies to 02-1, 08-1, and any quickstart caption.

5. **Exact volatile counts** ("13 merged PRs", "13/15 commits ahead", a specific
   v0.2.0 commit hash). VETOED in prose; cite the tag `v0.2.0`, not the hash or a
   count that rots (N00-5).

6. **`/spark:plan` described as creating real GitHub issues/milestones in v0.2.**
   02-4 is correct that this is ROADMAP (v0.3). VETOED if any quickstart asserts it
   as a shipped, mechanical capability; allowed only as labeled roadmap.

7. **`docs/PHILOSOPHY.md` (root-of-docs path).** VETOED as a target — ship
   `docs/explanation/philosophy.md` instead, to avoid an orphaned, mode-breaking
   placement. (Path-conflict ruling above.)

---

## Strongest positions (for the Editor)

- License is ONE P1 issue, not eight — and the veto stays armed until `LICENSE`
  carries a real grant.
- `list-skills` is ONE P3 issue across three surfaces; resist inflation.
- Pin the philosophy doc to `docs/explanation/philosophy.md`; the `docs/PHILOSOPHY.md`
  variant is vetoed to prevent an orphaned doc.
- Skill-layout conflict is a genuine P1 blocker; track the *decision* (an ADR),
  mark how-to and diagram as blocked-by.
- CI (04-2), GitHub topics / awesome-lists / og-image (10-x, 08-4), and a
  machine-readable doctor contract (02-6) are out of docit's shipping scope —
  route to launch-copy/backlog, do not seat as doc issues.
- Veto stands on: v0.3.0-as-shipped, codify/`.docit-notes`, MIT-as-adopted,
  doctor "OK"/tally, volatile counts, plan-creates-issues-in-v0.2, and the
  `docs/PHILOSOPHY.md` path.
