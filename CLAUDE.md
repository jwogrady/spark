# Spark — Claude Code Guide

@AGENTS.md

> `AGENTS.md` (imported above) is the single canonical agent contract for this
> repository, maintained with the `agents-md` skill — one body, so the two
> files can never drift apart. Everything Claude Code needs is there. Add a
> note below this line only when it is genuinely Claude-specific.
