# Release record — v0.22: Triage

> **Disposition: `Blocked`.** Certification ran against `fe382d6` and returned
> **19 of 19 behavioural checks green with one release-gate criterion failing**:
> issue #567 declares `docs-impact:reference` and its implementation touched no
> governed reference path, so not every v0.22 documentation declaration is
> satisfied. The remedy is a governed field on a live issue and therefore a human
> decision; it is recorded here rather than corrected, because an agent
> relabelling an issue to make a release gate green is the exact defect this
> milestone exists to prevent.
>
> No version has been cut. The roadmap says `In progress`, which is true.

Milestone: **#19 — v0.22 — Truth-first onboarding**. Release gate: **#479**.

Named product outcome: **Given an existing repository, Spark establishes what is
true without changing it, reconciles material contradictions through explicit
approval, preserves human-owned decisions as human-owned, and derives a coherent
course before normal development begins.**

## What shipped into the tree

Three read-only verbs and one authority boundary, in the order they were built:

| Issue | Outcome |
| --- | --- |
| #567 | The v0.21 ledger check anchored to the release interval it certifies, so v0.22 can add suites without falsifying v0.21's history |
| #559 | `DECISION REQUIRED` as a distinct verdict and exit code — an owed judgment is no longer reported as a mechanical failure |
| #467 | `spark triage` — the read-only truth pass, composing `orient`, `brief` and the governance engine rather than re-deriving them |
| #570 | Issue-body prose demoted to evidence; release-decision authority is a milestone or a governed disposition label |
| #571 | Issue references identified by the `#` sigil, so `v0.22` is not issue #22 |
| #468 | `spark reconcile` — the approval-gated slate, and the apply path where one approved group is one revertible commit |
| #587 | Disposition member names carried whole through `roadmap-check` |
| #469 | `spark course` — which objective is coherent next, distinct from where the lifecycle left off and from which issue to start |
| #590 | Every approval resolved against a freshly derived slate; a stale row never authorises a mutation |
| #592 | Member identity as one record in the per-issue validator and the plan compiler, so all three consumers of the model agree |

Five of those ten were found by dogfooding v0.22 against itself **after** the
capability they belong to had shipped. That is the milestone's real shape: the
tooling was pointed at the repository that builds it, and it kept finding places
where a guarantee was stated and not held.

## The two constraints, and where they were tested

**Unknown evidence is not human judgment.** Every verb carries them as separate
states, and a row whose evidence is unread carries no disposition at all.
Asserted directly in `test-triage-truth.sh`, `test-reconcile-slate.sh` and
`test-course-derivation.sh`, each with a negative control proving the collapse
would be caught.

**Recommendation is not authority.** An agent may propose a value with evidence;
the gap clears only when the governed field carries the decision. Asserted in
`test-governance-decision.sh` with the #558 incident as a three-state fixture,
and re-proved live during certification.

## Certification

Ran against `fe382d6`. Nineteen live observations, not a rerun of CI: each check
exercises the shipped behaviour and reads its result.

**19 passed, 0 failed.** Six negative controls broke a guarantee in the certified
tree and confirmed the certification noticed each one:

| Broken | Caught by |
| --- | --- |
| triage writes a file | C2 read-only |
| prose clears the release gate | C6 recommendation is not authority |
| approved groups batched into one commit | C8, C8b |
| a multi-word member split again | C17 |
| course resolves ambiguity itself | C12 |
| onboard skips triage | C1 |

Two certification checks were rewritten mid-run because they could not fail: one
compared row counts where it should have compared the rows, and one asserted a
vocabulary against a fixture that contained none of it. Both are recorded because
a check that passes without looking is the failure mode this project keeps
finding in its own instruments.

## Outstanding

- **#567's `docs-impact:reference`** has no qualifying evidence. A human decision.
- **#558** and **#564** carry no release disposition and remain outside v0.22 by
  decision. Neither was assigned a value to clear a gate.

## Suites

64 behavioural suites, 2392+ assertions, `spark doctor` 0 errors / 0 warnings,
`spark governance validate` with zero mechanically invalid findings.
