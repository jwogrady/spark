# v0.17.0 — Provenance promotion

**Status:** In progress; architecture decided (#374), hub resolution (#375)
and knowledge promotion (#376) merged.
**Milestone:** GitHub milestone #14 — `v0.17 — Provenance promotion` — the
version authority for this release.
**Release gate:** GitHub issue #373.
**Architecture:** ADR-0028.

## Shippable outcome

A Spark-managed spoke can remain lean while durable cross-project learning is deliberately promoted, with GitHub evidence and required human judgment, into one configured memory-hub repository.

The release is successful when the workflow works generically and is proven through Cosmos dogfood without making Cosmos a Spark dependency.

## Scope

| Order | Issue | Outcome |
|---|---|---|
| 1 | #374 | Decide the cross-project memory-hub/spoke architecture and extend ADR-0008 without introducing another global layer. **Closed by PR #378.** |
| 2 | #375 | Add one explicit project fact that resolves a spoke's memory hub; never guess or hard-code a platform. **Closed by PR #381.** |
| 3 | #376 | Extend `knowledge` to classify local versus durable cross-project learning, gather GitHub evidence, and promote through the destination's own rules. **Closed by PR #383.** |
| 4 | #377 | Surface the promotion question at natural lifecycle boundaries and prove positive, negative, and Cosmos dogfood cases. |
| 5 | #372 | Eliminate duplicate Release Please entries so v0.17 publishes one changelog entry per logical change. |
| 6 | #380 | Keep the release record truthful: the plan names the existing milestone; a documentation-truth check guards the claim. |
| Gate | #373 | Reconcile docs/evidence and release only when the complete outcome is demonstrated. |

## Dependency graph

    #374 architecture [done]
        |
        v
    #375 hub resolution [done]
        |
        v
    #376 knowledge promotion [done]
        |
        v
    #377 lifecycle + proof
        |
        v
    #372 release-note integrity
        |
        v
    #373 release gate

    #380 release-record truth (independent; feeds the gate's docs-truth item)

The graph is planning intent. If implementation proves a dependency is false, correct the GitHub record explicitly rather than preserving an artificial sequence.

## Ownership boundary

- GitHub: engineering evidence.
- Spark: promotion process.
- Memory hub: durable cross-project meaning.
- Spoke: implementation and local release truth.
- Runtime: observed operational truth.
- Human: architectural judgment and release approval.

## Release proof

Before v0.17.0 is eligible for Release Please:

1. A positive fixture demonstrates a durable boundary discovery being classified and promoted with evidence.
2. Negative fixtures demonstrate routine engineering work terminating with no hub write.
3. A real spoke-to-Cosmos dogfood run uses configured hub resolution and Cosmos's existing review/placement rules.
4. No Spark code or docs require Cosmos, Status26, Prime, or repository naming conventions.
5. The five public lifecycle stages remain unchanged.
6. `spark doctor` and relevant behavioral/evaluation tests are green.

## Non-goals

No provenance database, repository synchronization, automatic decision acceptance, bulk Git-history copying, or per-commit promotion gate.

## Milestone record

GitHub milestone #14 — **`v0.17 — Provenance promotion`** — exists and is the
version authority for this release; #372–#377 are assigned to it, and #380
(filed against the stale draft of this section) joins the release scope.
Transient tooling constraints are session facts, not release truth: this plan
records what is, and `tests/test-release-plan-truth.sh` guards release plans
against carrying an environment-capability claim forward after it stops being
true (issue #380).
