# Release record — v0.21: Governance as schema

> **Disposition: `Shipped`.** `v0.21.0` was published 2026-08-28T15:54:21Z at
> `bdbdd80`, by a human approving the release review, closing gate #478, and
> merging Release Please PR #506. Every admitted repair had landed and every
> reopened issue had been behaviourally re-audited and reclosed first.
>
> This record has been `Blocked` twice — once when certification was withdrawn,
> and again when a contract review found #530, #524 and #473's promise unmet. Both
> episodes are kept below rather than overwritten. A release record that erases
> its own retractions is the thing this milestone spent its length learning not to
> produce.
>
> This record's earlier disposition was `Blocked`, and the history is kept below
> rather than overwritten — a release record that erases its own retraction is
> the thing this milestone spent its length learning not to produce.

Milestone: **#18 — v0.21 — Governance as schema**. Release gate: **#478**.

Named product outcome: **One machine-readable governance contract can be
inspected, diffed, provisioned, validated, and used to compile approved plans
into GitHub state.**

Spark taught repository governance partly through prose and partly through
individual commands, so an agent could hold the doctrine correctly and still
recreate labels, priorities, milestone conventions and metadata rules
inconsistently from repo to repo. This release replaces that with one versioned
authority for **allowed shapes** — never for project-specific judgment.

## Scope

Eighteen issues: the five original, plus the thirteen repairs admitted after
certification was withdrawn. #483 and #473 each appear twice — once as
original scope and once as a repair cycle, their second re-audits.

| Issue | Outcome | Merge |
| --- | --- | --- |
| #470 | The versioned canonical governance schema, resolved through the existing preference tiers | `710421b` (PR #505) |
| #483 | `docs-impact` as a schema-defined label family, with its grammar and evidence validator | `3a58b0d` (PR #507) |
| #471 | Deterministic governance `inspect`, `diff`, `apply`, `validate` | `1d073fc` (PR #509) |
| #472 | The plan compiler: milestone creation, issue update, schema-validated taxonomy, cycle detection, diff-vs-live, verify | `ba4964b` (PR #513) |
| #473 | Integration into `doctor`, selection, `brief`/`resume`, and new-repo provisioning | `8c3c7c3` (PR #514) |

**The repairs**, each with its own behavioural suite and each with measured
discrimination — the fix was reverted and the reds counted, so no assertion in
this table is claimed without evidence that it can fail:

| Issue | Defect repaired | Merge |
| --- | --- | --- |
| #511 | `exclusive` was one fact per family+member, so an override sat beside the shipped rule and consumers read different ones | `d9eda4f` (PR #523) |
| #470 | re-audited: all twelve criteria, and the reopening's own scenario, driven behaviourally | `31e1cc3` (PR #525) |
| #512 | a failed linked-PR lookup was indistinguishable from an empty one, so `docs-impact:none` PASSed on incomplete evidence | `28958fc` (PR #527) |
| #483 | re-audited: every grammar row, plus three coverage gaps the audit exposed | `facd547` (PR #528) |
| #515 | a malformed `update` target validated locally and failed only after earlier creates had landed | `4838343` (PR #529) |
| #517 | `plan verify` checked title and labels only, and certified milestone, body, hierarchy, dependencies and order it never read | `d6301a5` (PR #531) |
| #518 | order placement used one global `after_id` chain, so it leaked across unrelated parents | `b6e1861` (PR #532) |
| #472 | re-audited; found and fixed a whole-word entry in a list of stems, so "for sequencing" wired a real prerequisite | `1a87d5c` (PR #533) |
| #520 | `spark next` warned that readiness was unassessable and then printed a route at exit 0 | `241d3d7` (PR #534) |
| #473 | re-audited; surfaced a semantic decision (#535) rather than settling it | `49e00bb` (PR #536) |
| #521 | the ROADMAP headline named a superseded baseline, and no check could see it | `501c06d` (PR #537) |
| #516 | a local refusal required `gh`, so the suite failed on a supported zero-`gh` host | `e12ac7e` (PR #538) |
| #540 | `plan verify` read the body path as the milestone when a record omitted its milestone — a regression in this run's own #517 fix | `3cdcf56` (PR #543) |
| #541 | the roadmap headline guard could certify the contradiction it existed to prevent, by taking the greatest version mentioned anywhere | `e4b7ac0` (PR #544) |
| #546 | the record contradicted itself about its own size — two of four counts updated in one commit — now checked by deriving every count from the documents' structure | `8fd8e24` (PR #547) |
| #530 | `docs-impact` read one `first: 50` page of linked PRs and graded a truncated set as complete, so #483's "aggregates across **all** linked implementation PRs" did not hold | `9b3a604` (PR #550) |
| #524 | the generated Release Please PR entered the *implementation*-evidence set, because GitHub reports it as a closing reference for every issue in the release | `00c74d3` (PR #551) |
| #483 | re-audited after #530 and #524, with pagination proven by a genuine two-page connection rather than iteration over two nodes | `13a8520` (PR #552) |
| #473 | its new-repo promise overpromised — it claimed setup provisions templates that are human-provisioned by design; the promise narrowed and the provisioning boundary became machine-readable | `adf4405` (PR #553) |
| #554 | the release-automation exclusion matched a branch prefix alone, so a human PR sharing that prefix lost its evidence — a defect in #524's own fix | `1c4f6e7` (PR #555) |
| #549 | the ledger's suite counts were stale and the truth guard reported agreement beside them, because it did not cover those rows | `1e9251b` (PR #556) |
| #526 | `hooks.md` claims the guard tokenizes rather than substring-matches, and it refused `echo`, `grep` and heredocs that merely quoted a push — a shipped guarantee that was false | `c2fd01c` (PR #557) |

The last two were filed **after** the first certification and were defects in code
this run had already merged. `milestone-gate` blocked the release while the
certification comment still said the criteria held — the workflow was right, and a
certification is evidence for a decision rather than a substitute for the gate.

## What the milestone implemented

Everything below shipped in `v0.21.0`. The guarantees were **falsified** by the
findings in the retracted-certification section and repaired before the release;
each annotation names the repair that closed it.

**A versioned governance artifact** —
`preferences/governance-models/spark-default.tsv`. Label families with their
cardinality and requirement; execution structure with an explicit
`authoritative`/`derived` verdict per surface; the separations that must not be
collapsed; provisionable surfaces; mechanically assessable enforcement. It is
line-oriented and tab-separated rather than nested JSON, for the reason
[ADR-0030](../adr/0030-governance-model-representation.md) records: the
preference resolver is deliberately flat, and the zero-dependency rule means a
nested-JSON authority would be unreadable wherever `jq` is absent — "degrades
gracefully" cannot mean "degrades to no governance model at all". Verified with
a `jq`-free `PATH`.

**Resolution keeps the three tiers**, later winning, every record naming its
source. A tier declaring any member of a family replaces that family's whole
member set, because a per-member merge can only add and never remove. Removing a
member prunes the lower tiers' rules about it rather than bricking the model.

**`docs-impact`** — every issue declares what documentation its change affects,
and the declaration is verified against the issue's implementation evidence. The
failure mode targeted is not "a PR changed no docs"; it is **silent** docs
impact, where an omission is indistinguishable from a deliberate "none".
Evidence is the union across every merged-or-open linked PR, aggregated then
judged, from the paginated files endpoint and including rename sources.
**Not yet closed — #512:** when linked-PR retrieval fails, branch mode grades
partial evidence as PASS, so silent impact remains reachable.

**The governance engine** — `inspect`, `diff`, `apply`, `validate` over one row
generator, so that a rule cannot have two authorities and the four cannot
disagree. `spark labels` is an explicit compatibility wrapper over the same rows.
**Falsified, then repaired — #511:** a cross-tier `exclusive` override leaves one rule with two
answers, because consumers disagree about which member is exclusive.

**The plan compiler** — `issue-manifest.sh` extended, not replaced, with
`milestone`, `update`, `order` and `decision` records, plus cycle detection and
a live diff; `spark plan validate|diff|apply|verify` is its command surface.
Structure stays the script's, meaning the schema's.
**Three guarantees do not hold:** #515 — a malformed update target passes
validation and fails only after earlier creates have landed; #517 — `verify` can
PASS without checking body, hierarchy, dependencies or preferred order; #518 —
order sequencing leaks across unrelated sub-issue parents.

**Integration** — `doctor` reports governance readiness and names what it
defers; selection refuses mechanically invalid execution metadata; `brief` and
`resume` surface readiness from deterministic state.
**Falsified, then repaired — #520:** when the governance model does not resolve, `spark next`
reports readiness NOT ASSESSED and then routes the work anyway, exiting 0.

## The self-hosting rule, and how it was made satisfiable

This milestone introduces `docs-impact` and is the first held to it. **#470 is
the sole bootstrap exception** — it had to close before the family could exist.

The gate initially could not satisfy its own rule. Its sub-issue order placed
#483 last, and `spark next` reads that order as the tie-breaker among equally
prioritized eligible issues — so #471, #472 and #473 would all have closed
before `docs-impact` existed, while the rule named exactly those three. The
order was corrected to `470, 483, 471, 472, 473` on 2026-08-27 with the native
dependency graph and every priority left untouched, and the reconciliation is
recorded on #478. (That order has since been extended with the repair issues
admitted to the milestone; the gate's current sub-issue list is the live
authority, not this snapshot.)

The result is verifiable rather than asserted: #483 closed before #471, #472 and
#473, and each of those four carries `docs-impact:reference` satisfied against
its own branch. Nothing was retrofitted.

## What the withdrawn certification checked, and where it was hollow

Recorded as-was, because the gap between these claims and the findings above is
the useful part. Verified against merged `master` at `8c3c7c3` — but see the
annotations:

- **Scope** — every leaf closed; only the gate remains open. **No longer true:**
  #470, #483, #472 and #473 have since been reopened.
- **Schema** — a `version` record resolves; `governance.model` selects it as a
  scalar preference rather than a fourth configuration layer; all three tiers
  are read and reported.
- **Allowed shapes only** — the shipped artifact contains no issue numbers and
  no per-issue verdicts.
- **Read-only** — `inspect`, `diff` and `validate` leave the working tree
  untouched; `apply` previews and needs `--yes`; drift repair needs
  `--repair-drift` on top of that; a judgment row is never applied.
- **One authority per rule** — `spark labels` delegates to the shared row
  generator; `spark plan` delegates to the one compiler. **Falsified by #511:**
  a cross-tier `exclusive` override leaves one rule with two answers, because
  consumers disagree about which member is exclusive.
- **`docs-impact`** — family and path mapping are schema data; the labels are
  provisioned on the remote.
- **Self-hosting** — #471, #472, #473, #483 each declare and satisfy a
  disposition; #470 declares none, as the recorded exception.
- **v0.20 shipped first** — `v0.20.0` is an ancestor of `master`.
- `spark doctor` reports 0 errors. `tests/run.sh` passes all 48 suites **in an
  environment that has `gh`** — which is the caveat criterion 13 does not carry,
  and #516 is the consequence.

**Where it was hollow.** The criterion-10 check grepped for a function name and
a comment string, so it verified that the words were still present rather than
that the properties held — #515 and #518 both violate exactly those properties.
The read-only checks confirmed the verbs do not *write*; nothing confirmed that
a PASS *means* anything, which is #512, #517 and #520.

## Known state, reported rather than hidden

`spark governance validate` **fails on this repository**, and that is the
correct answer:

- **#508 carries two category labels** (`bug` and `infrastructure`) and has no
  release disposition.
- **Milestone-assigned issues predating `docs-impact`** carry no disposition.

Both are real governance findings about the backlog, not defects in the check.
Spark reports them and refuses to guess; deciding each one is the human's.

Seven category labels also **drift** from the model — they were created before
the schema existed, so their colours and descriptions never matched. Reported as
`~` and deliberately not repaired: an existing label is the project's own
decision, and repair needs `--repair-drift` said out loud.

## Defects found during the release's own validation

Recorded because the count is the useful part. Across five implementation
cycles, adversarial review and self-attack found **31 reasoning defects** in new
code, while Spark's deterministic gates caught **12 mechanical omissions** —
missing reference sections, line and byte budgets, a tier-boundary violation, a
commit subject over length, and a shipped/shipped taxonomy drift. The split was
one-directional: not one gate caught a reasoning defect, and not one review
finding was a mechanical omission.

Two classes are worth naming because they recurred *after* being fixed and
documented:

- **A green result that means "not looked at."** Found in `apply`, then shipped
  again in `verify`, then again in doctor's file-surface count — three separate
  instances across three cycles, each after the previous had been fixed and
  written up.
- **An assertion that cannot fail.** Five instances, including one where the
  test written to prove a behaviour was what concealed its regression.

Certification itself found one more: the trunk-policy surface was emitted as
"not assessed", and `apply`'s refusal on any unread surface then disabled the
verb entirely. A deliberate deferral to a named verb is not an unknown; it has
its own status now, and `apply`'s guard is scoped to the surface it acts on.

## Release discipline

No v0.21 branch merged to `master` before `v0.20.0` was published. Every change
was issue-backed, branched, reviewed and merged through its own pull request
with a normal merge commit; nothing was squashed and no history was rewritten.

Release Please PR **#506** proposes `v0.21.0` and **must not be merged** in this
state. Its notes reconcile against the range — one `0.21.0` section from the
`v0.20.0` boundary, and 33 visible release-note commits against 33 unique
generated entries — but note that entry counting alone would not have detected
an *omission*; the commit-count reconciliation is what establishes that.

## Gate certification — withdrawn

A certification was posted on **#478** and then **retracted**. It reported 36
checks passing while five gate criteria were in fact false, so it certified
nothing. Why it failed is recorded in
[the evaluation ledger](../ops/v0.21-dogfood-evaluation.md): presence and
environment checks stood in for behavioural evidence.

### Release-blocking findings

| Finding | Falsifies | Owner issue |
| --- | --- | --- |
| #511 — cross-tier `exclusive` overrides split governance consumers | criterion 6 (no rule with two authorities), criterion 2 (deterministic tier resolution) | #470 |
| #512 — `docs-impact` branch mode grades partial evidence as PASS | criterion 7 (grammar implemented exactly) | #483 |
| #515 — malformed update targets pass validation, fail after remote creates | criterion 10 (validation before any call) | #472 |
| #517 — `plan verify` can PASS without checking most mutation-bearing facts | #472's "applied state is verified after mutation" | #472 |
| #518 — order sequencing leaks across unrelated sub-issue parents | criterion 10 (no partial mutation) | #472 |
| #520 — `spark next` routes work after readiness is NOT ASSESSED | #473's execution-readiness guarantee | #473 |
| #516 — the plan compiler suite fails without `gh` on `PATH` | criterion 13 (`tests/run.sh` passes) | gate #478 |
| #521 — ROADMAP named `v0.19.0` as the published baseline | criterion 12 (truthful roadmap) | gate #478 |

#510 was found by the certification itself and is **fixed** (`eabff38`),
re-verified behaviourally against `master`. #508 is **legitimate backlog**,
provably inert for this range: 33 visible release-note commits against 33 unique
generated entries, with no carrier declaration in the range capable of
collapsing it.

### What has to be true before this record can claim readiness

Each finding above closed on behavioural evidence; #470, #483, #472 and #473
behaviourally re-audited against the repaired implementation and truthfully
reclosed; the full suite passing **both** in a normal environment and with `gh`
absent from `PATH`; and every #478 criterion re-audited with evidence of
behaviour rather than of symbols. **The release act remains a human merging the
Release Please PR, and that is not yet in prospect.**
