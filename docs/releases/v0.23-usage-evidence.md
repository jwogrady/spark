# v0.23 usage and field-evidence report

> **Status:** In progress — v0.23 is **not released**. The release gate
> [#480](https://github.com/jwogrady/spark/issues/480) is open and closes last;
> final release approval is human-owned. This report is the durable
> release-evidence deliverable required by
> [#709](https://github.com/jwogrady/spark/issues/709); it records evidence, it
> does not certify the release.

## Purpose

Before v0.23 ships, this report gathers — in one pointable place — where Spark
has actually been used, what it materially contributed, which released control
plane governed the work when that is provable, what was measured, and where
Spark failed or got in the way. Every material claim links to durable
GitHub/repository evidence so a reader can verify it cold rather than
reconstructing it from dozens of objects.

It is deliberately **not** a success story. Released-v0.22 consumer usage is kept
separate from unreleased-v0.23 self-host dogfood; governor provenance is kept
separate from execution provenance; consumer outcomes are kept separate from
Spark's contribution (correlation is not attribution); and failures, friction,
and `NOT ASSESSED` gaps are preserved alongside the wins.

## Reporting vocabulary

```text
Governor   = released Spark vX.Y.Z control plane that actually governed the work
Execution  = role/model/provider/surface/run facts proven by durable evidence
Work unit  = repository + issue/PR/run/HEAD being described
Outcome    = observed result/verdict, not authority
```

**Governor evidence class** is one of:

- **CANONICAL** — a #710 `Spark-Governed-By: vX.Y.Z` commit trailer / durable
  PR-level governor fact, sourced from the installed released governor.
- **HISTORICALLY EVIDENCED** — the released governor is established by
  independent durable evidence (dates, release windows, attested prose), but
  predates #710 so no canonical trailer exists.
- **NOT ASSESSED** — the released governor cannot be mechanically established;
  reported as unknown, never fabricated.

The governor is **never** inferred from the target repo's working-tree version,
the release being developed, Spark files merely being present, an installed
integration alone, provider/model identity, or conversation memory. No
historical commit or PR is rewritten and no retroactive governor metadata is
fabricated. The pre-#710 absence of canonical governor provenance is itself
legitimate negative evidence.

**Governor timing anchor.** The #710 primitive can canonically record **any**
installed, pinned released governor — v0.22.0 included, **not only v0.23** — but
it records one only when such a governor is actually installed and stamping the
work. Released **v0.22.0** was published **2026-08-30T14:25:41Z** (tag `v0.22.0`,
target commit `f364d42`,
[release](https://github.com/jwogrady/spark/releases/tag/v0.22.0)); the primitive
landed in the tree via PR
[#712](https://github.com/jwogrady/spark/pull/712) (merged
**2026-09-03T19:13:47Z**, `8a49ed5`). **None of the work in this report was
produced under an installed, pinned governor:** verified directly — no listed
commit or PR carries a `Spark-Governed-By` trailer or a `Governed by Spark
vX.Y.Z` line (the #712 PR body only *describes* the mechanic). Governor
attribution here is therefore **HISTORICALLY EVIDENCED** (external evidence such
as a release window plus attested prose) or **NOT ASSESSED** (unknown), never
CANONICAL — but because no governor fact was stamped, **not** because the work
predates any particular release.

---

## Evidence class 1 — released-v0.22 consumer usage (Business Solutions Depot)

The one durable released-Spark consumer engagement: an autonomous website
execution run in `jwogrady/cosmic-businesssolutions` on **2026-08-31**, operated
under released Spark **v0.22.0**. Because the whole engagement (issue #171
created 2026-08-31; PRs #170–#175 merged 2026-08-31 16:53–20:15Z; #160 closed
2026-08-31 20:15Z) falls **after** the v0.22.0 release (2026-08-30) and before
any v0.23 release, the governor is **HISTORICALLY EVIDENCED** as v0.22.0 (attested
in the [#171](https://github.com/jwogrady/cosmic-businesssolutions/issues/171)
handoff and the upstream dogfood report
[Spark #679](https://github.com/jwogrady/spark/issues/679)); no `Spark-Governed-By`
trailer exists, and per-commit governor attribution on the merge SHAs is
**NOT ASSESSED**.

| Work unit | State | Outcome | Durable link |
|---|---|---|---|
| `cosmic-businesssolutions#171` orchestration: autonomous website execution | OPEN | Coordination/handoff; end-of-run master `913cf58`; records closed #160/#90/#85/#84/#101 and merged PRs #170/#172/#173/#174/#175 | [#171](https://github.com/jwogrady/cosmic-businesssolutions/issues/171) |
| `cosmic-businesssolutions#160` five commissioned homepage images | CLOSED 2026-08-31 | Delivered after integration + rendered specialist verification (11 image generations for 5 finals; 4 finish-gate passes) | [#160](https://github.com/jwogrady/cosmic-businesssolutions/issues/160) |
| PR #170 VideoObject on four service pages | MERGED `78954d0` | Merged under released control plane | [#170](https://github.com/jwogrady/cosmic-businesssolutions/pull/170) |
| PR #172 photograph captions | MERGED `d9d10bd` | Merged | [#172](https://github.com/jwogrady/cosmic-businesssolutions/pull/172) |
| PR #173 localise photograph filenames | MERGED `575ffe9` | Merged | [#173](https://github.com/jwogrady/cosmic-businesssolutions/pull/173) |
| PR #174 five homepage slider photographs | MERGED `713441c` | Delivers #160's finals | [#174](https://github.com/jwogrady/cosmic-businesssolutions/pull/174) |
| PR #175 slide-3 crop / tangency fix | MERGED `913cf58` | Corrects a zero-pixel tangency defect | [#175](https://github.com/jwogrady/cosmic-businesssolutions/pull/175) |

**Governor evidence class:** HISTORICALLY EVIDENCED (v0.22.0). **Execution
evidence:** the actor was an AI coding agent operating the released control plane;
precise model/provider/run identity for these specific PRs is **NOT ASSESSED**
(no durable per-work-unit execution record was located — the 10 most recent
`validate` Actions runs in the consumer repo are dated 2026-09-01, the day after
the engagement, and their head SHAs do not match the #170–#175 merges, so they do
not prove the machine path for these PRs).

**What released Spark demonstrably contributed** (from #679, corroborated by the
#171 handoff — Spark's contribution, distinct from the consumer's design outcome):

- **Commit-message enforcement refused invalid commits, which were then
  corrected** — the installed commit-msg hook rejected an AI `Co-Authored-By`
  trailer the agent's own defaults tried to add, and subjects over the 72-char
  limit (including a later 74-character subject). The agent reported changing its
  commits because Spark refused them.
- **`spark state --set` failed atomically on invalid state** — rejected an
  invalid key rather than leaving a partial record.
- **`spark brief --short` prevented repeated rediscovery** of settled facts
  (release mode, stack, preference posture) across issues.
- **Governance vs execution authority stayed distinct under a long run** — v0.22
  governed while paid commissioned-media spend remained a human-owned Crossroad.

**Negative / friction evidence from the same engagement** (see the Defects
section for owners):

- **Guard false-positive** — `guard-bash.sh` blocked a legitimate feature-branch
  push because the same compound command also contained `gh pr create --base
  master`; the word `master` belonged to the PR-base argument, not the push
  refspec. Worked around by splitting the commands.
- **`spark next` unmilestoned-selection limitation** — `spark next → NOT ASSESSED`
  for the whole run because the consumer repo had no open milestone with open
  issues; the operator had to become the selection layer.
- **What Spark did not find** — the strongest quality findings (a copy-width rule
  colliding with a generated terminal, a missing vertical-overlap composition
  contract, an Astro selector-specificity defect, the zero-pixel tangency fixed
  by PR #175) came from rendered browser evidence and specialist review, **not
  Spark**. This is boundary evidence, not asserted as a Spark defect.

---

## Evidence class 2 — unreleased v0.23 self-host / live proof

This is Spark developing Spark (unreleased `master`), kept separate from consumer
usage. It exercises v0.23 optimization primitives but is **not** shipped v0.23
autonomy: the independent reviewer lane (#584), the reviewer→writer loop (#585),
and release-certification orchestration (#608) are **v0.24-deferred** and must not
be read as v0.23 scope.

### Independent reviewer repair cycles (`CHANGES REQUIRED → repair → new HEAD → PASS`)

Representative durable review-cycle progressions (the `spark-openai-review pr=…
head=… verdict=…` markers are durable PR comments). **Governor evidence class:
NOT ASSESSED for all six.** Verified directly: none of the six merge commits
carries a `Spark-Governed-By` trailer, and none of the six PRs carries a durable
`Governed by Spark vX.Y.Z` governor line (the mentions in #712's body are prose
*describing* the mechanic, not a stamped governor fact). Pre-#710 timing proves
only that canonical provenance was *unavailable*; it does not name the governor,
and no independent per-work-unit evidence establishes it — so the released
governor for each cycle is reported as unknown rather than inferred from a date
window. This includes #712 (#710) itself, whose commits were not stamped by an
installed, pinned governor even though they introduced the mechanism, and #717
(#670), whose merge `3a454e2` postdates #710's merge `8a49ed5` yet still carries
no trailer. These cycles are cited here for the **review-timing and
one-review-per-HEAD** evidence they carry — governor attribution is not among it.

| PR (issue) | Cycle recorded on the exact HEADs | Merged HEAD verdict | Merge |
|---|---|---|---|
| [#708](https://github.com/jwogrady/spark/pull/708) (#665) | NOT ASSESSED → 6× CHANGES REQUIRED → **PASS** | PASS (`a8d6776`) | `db3e08b` |
| [#712](https://github.com/jwogrady/spark/pull/712) (#710) | 8× CHANGES REQUIRED → **PASS** | PASS (`546ed40`) | `8a49ed5` |
| [#716](https://github.com/jwogrady/spark/pull/716) (#666) | CHANGES REQUIRED → **PASS** | PASS (`5807cc9`) | `f5a81b4` |
| [#707](https://github.com/jwogrady/spark/pull/707) (#664) | DECISION REQUIRED → **PASS** | PASS (`67ed3db`) | `b24c170` |
| [#706](https://github.com/jwogrady/spark/pull/706) (#648) | CHANGES REQUIRED → **PASS** | PASS (`c732e85`) | `b9f34f5` |
| [#717](https://github.com/jwogrady/spark/pull/717) (#670) | **DECISION REQUIRED** (no PASS marker on record) | **DECISION REQUIRED** (`33fa4fc`) | `3a454e2` |

These cycles are the strongest evidence that the v0.23 review-timing and
one-review-per-HEAD primitives were genuinely exercised: each new HEAD invalidated
prior review evidence and was re-reviewed, and the merged HEAD in five of six
cases carries a recorded reviewer PASS. **Friction / negative:** #717 (#670) is
the exception — the merged HEAD `33fa4fc` carries a **DECISION REQUIRED** verdict
with no recorded PASS. That verdict was dispositioned by durable authority
([#480](https://github.com/jwogrady/spark/issues/480) admits #670 as v0.23 scope;
[#677](https://github.com/jwogrady/spark/issues/677) selected #670 as the lane) as
a **false Crossroad** — a stale native gate-hierarchy state (#670 not yet a
sub-issue of #480), repaired by attaching #670 under #480 — rather than a genuine
product decision. It is recorded here as friction: the merged runtime-telemetry PR
does not carry a clean exact-HEAD reviewer PASS marker.

### Claude coding-lane live proof (#583)

[Issue #583](https://github.com/jwogrady/spark/issues/583) is CLOSED, stating the
Claude coding lane is live-proven end to end (`resolve → claude → publish`).
Durable machine fact: Actions run
[33585855798](https://github.com/jwogrady/spark/actions/runs/33585855798) —
workflow "Claude Code", conclusion **success**, head `1d21722`, 2026-09-02,
`issue_comment` event. The run conclusion/date/SHA are directly verified GitHub
facts; the claim that it proves the full lane semantic is asserted in the #583
body (governor evidence class does not apply — this is execution/lane proof, not
governor provenance). This is v0.23-development dogfood; the full autonomous
reviewer→writer product is v0.24.

### Governor-provenance primitive (#710) — CANONICAL going forward

[#710](https://github.com/jwogrady/spark/issues/710) (CLOSED/COMPLETED, PR
[#712](https://github.com/jwogrady/spark/pull/712) merged `8a49ed5`) shipped the
canonical governor primitive to `master`: the commit-msg hook stamps exactly one
`Spark-Governed-By: vX.Y.Z` from the **installed** governor's own `spark version`
(repo-local `spark.governorBin` pin), never the target's unreleased manifest;
idempotent; fails closed on a conflicting supplied trailer; leaves ungoverned
repos untouched; a durable PR-level `Governed by Spark vX.Y.Z` fact survives
squash/rebase. The **AI `Co-Authored-By` ban is preserved** — Claude/OpenAI remain
delegated workers, never authors — and release-note generation ignores the
trailer. Test evidence in the PR: `test-commit-msg` 30/0, `test-governance-provenance`
8/0 (an end-to-end case where a working tree advertising v0.24.0 is stamped
`Spark-Governed-By: v0.23.0`), `test-release-notes-runner` 55/0. The governor it
records is **whichever released governor is installed and pinned** — v0.22.0 as
readily as v0.23.0 — not specifically the release being developed. This means
**only work committed while an installed, pinned released governor stamps it can
carry CANONICAL governor provenance** — and none of the work in this report does
(no listed commit or PR carries the trailer or line, verified).

### Runtime-footprint telemetry truth (#670) — CANONICAL semantics

[#670](https://github.com/jwogrady/spark/issues/670) (CLOSED/COMPLETED, PR
[#717](https://github.com/jwogrady/spark/pull/717) merged `3a454e2`) corrected the
run-footprint telemetry so a multi-command run is truthful: each invocation
**appends** `<bytes>\t<modules-or-none>\t<iso8601>` to an append-only
`.spark/telemetry/<run>.footprint` log, and reads **derive** the summary —
`runtime_peak_source_bytes` (the peak single-invocation bytes, monotonic) and
`runtime_modules_loaded` (the distinct union of modules loaded). **Semantic
guard for this report:** these are a *run summary* (a peak and a set), **not**
richer per-actor or per-command execution provenance; they must not be
reinterpreted as such.

---

## Evidence class 3 — measured execution evidence

### The #476 dogfood baseline (recorded under #574/#476)

A single bounded implementation run (the #476 provenance-leakage work, PR
[#633](https://github.com/jwogrady/spark/pull/633) merged `c04de15`) is the one
durable execution baseline:

- **~35 minutes elapsed, ~53.5k tokens**, full behavioral suite **69/69**,
  repo-binary doctor healthy, docs-impact **PASS**, CI green
  ([#476 comment](https://github.com/jwogrady/spark/issues/476#issuecomment-5470452578);
  [#574 comment](https://github.com/jwogrady/spark/issues/574#issuecomment-5470453534),
  which also records focused suite 19/19).
- Preserved friction in the same run: a false-positive corrected by broadening
  provenance-anchor handling; repeated verification; a local/CI **doctor
  mismatch** caused by validating the released plugin instead of the working
  repository; a deterministic final defect (a `SKILL.md` at 104 lines vs the
  100-line budget).

**Classification: HISTORICALLY EVIDENCED.** These are self-reported dogfood
figures in issue comments, not mechanically-emitted HEAD-bound telemetry, and the
token/time figures are approximate (`~`), not provider-instrumented — which is
exactly the `NOT ASSESSED` posture #574's own contract requires when provider
instrumentation is absent. **It is a single baseline with no comparable
after-measurement.**

### Hot-path / suite-runtime measurement (#609)

[#609](https://github.com/jwogrady/spark/issues/609) (CLOSED/COMPLETED) measured
hot paths (best-of-3): `governance validate` ~2371 ms, `doctor` ~2117 ms,
`footprint` ~636 ms, `brief --short` ~75 ms, with `governance validate` gated at
4000 ms; slowest suites `test-latency` ~14 s, `test-course-derivation` ~11 s,
`test-governance-integration` ~11 s
([reopen comment](https://github.com/jwogrady/spark/issues/609#issuecomment-5472235359)).
It also preserves a **process failure**: #609 was closed prematurely by a
truncated read that never saw its acceptance criteria, then **reopened**. #609
itself flags that its reproducible baseline script "was scratch and never
committed" and that the post-change comparison was "argued in the PR body, not
produced as evidence."

**Classification: HISTORICALLY EVIDENCED with self-declared gaps.** Point
measurements exist; a reproducible before/after comparison does **not**.

---

## Evidence class 4 — #480 representative-workload map

[#480](https://github.com/jwogrady/spark/issues/480) requires five representative
workloads plus a two-dimension improvement claim. Each is mapped below with its
measurement fields — **elapsed, tokens/cost, checks, tool/API calls, failing-set
movement** — stated as a durable value where one exists and **NOT ASSESSED** where
it does not. No workload has a comparable before/after pair.

**1. Normal implementation/review cycle** — HISTORICALLY EVIDENCED (single
baseline). The #476 provenance-leakage run (PR
[#633](https://github.com/jwogrady/spark/pull/633), `c04de15`), recorded under
[#476](https://github.com/jwogrady/spark/issues/476#issuecomment-5470452578)/[#574](https://github.com/jwogrady/spark/issues/574#issuecomment-5470453534):

- Elapsed **~35 min** (self-reported, approximate). Tokens **~53.5k**
  (self-reported); cost **NOT ASSESSED** (no provider instrumentation).
- Checks: full behavioral suite **69/69**, focused **19/19**, doctor healthy,
  docs-impact PASS, CI green.
- Tool/API calls **NOT ASSESSED**. Failing-set movement **NOT ASSESSED** (run
  converged to green; no per-iteration failing count recorded).

**2. `CHANGES REQUIRED` repair cycle** — HISTORICALLY EVIDENCED as a workload;
measurement fields largely NOT ASSESSED. Durable per-HEAD reviewer-verdict
progression, e.g. #712 (8× CHANGES REQUIRED → PASS), #708 (NOT ASSESSED → 6×
CHANGES REQUIRED → PASS), #716, #706:

- Checks: on each merged HEAD, `docs-truth` + `doctor` + `gate` + `tests` all
  green (durable in `gh pr checks`). Failing-set movement: proxied by the
  reviewer-verdict progression per HEAD (durable), but **not** a numeric
  failing-test count.
- Elapsed, tokens, cost, tool/API calls per cycle: **NOT ASSESSED** (no durable
  per-run telemetry was emitted for these cycles).

**3. Full certification/release check** — latency HISTORICALLY EVIDENCED (point
measurement); the rest NOT ASSESSED. The closest durable numbers are #609's
hot-path timing (best-of-3): `governance validate` ~2371 ms, `doctor` ~2117 ms,
`footprint` ~636 ms, `brief --short` ~75 ms — with #609 itself flagging **no**
committed reproducible baseline and **no** before/after comparison. Tokens/cost,
tool/API calls, and failing-set movement for a full certification pass:
**NOT ASSESSED**.

**4. Small PR review** — **NOT ASSESSED** on every field (elapsed, tokens/cost,
checks, tool/API calls, failing-set movement); no durable HEAD-bound run summary
maps to this workload.

**5. Triage on an existing repository** — **NOT ASSESSED** on every field; no
durable run summary maps to this workload. (The BSD `spark next` friction shows
selection/triage was exercised in the field but produced no measurement record.)

**Improvement claim (≥2 dimensions without weakening correctness): NOT ASSESSED.**
No comparable before/after measurement pair exists for any workload, so no
dimension's improvement is evidenced; #480's acceptance checkbox stays unchecked.

---

## What Spark demonstrably helped

- Enforced authorship/commit discipline that a working agent then complied with
  (commit-msg refusals; the preserved AI `Co-Authored-By` ban).
- Kept governance authority distinct from execution under a long real
  engagement, with paid spend remaining human-owned.
- Reduced rediscovery of settled project facts (`spark brief --short`).
- Exercised, in self-host, a genuine one-review-per-HEAD repair discipline where
  each new HEAD received fresh review (five of six merged HEADs carry a reviewer
  PASS).
- Landed two telemetry-truth corrections (#665 execution counts, #670 runtime
  footprint) and the canonical governor primitive (#710), each with green suites.

## What Spark demonstrably did not help / does not own

- Spark did **not** surface the consumer's substantive visual/design defects;
  rendered browser and specialist review did. Spark's contribution there was
  process discipline, not quality detection.
- The `spark next` selection layer did not function for an unmilestoned consumer
  repo; the human became the selection layer.
- A compound-command guard false-positive blocked legitimate work.
- No mechanically-emitted, provider-instrumented execution provenance
  (model/provider/tokens/cost) exists for the consumer engagement; that richer
  role/model/provider/surface provenance is #711 (v0.24), explicitly **not** a
  v0.23 dependency and not claimed here.

## Defects discovered by dogfood and their owner / disposition

| Finding | Owner / disposition |
|---|---|
| Guard false-positive: protected-ref text in a *different* argument of a compound command | Not owned by #564/#675 on inspection; flagged in #679 as an unowned guard-scoping case (see also the #676 guard-scoping-friction draft) — **open, needs an owner** |
| `spark next` NOT ASSESSED for an unmilestoned repo; operator became selection layer | Candidate owners flagged in #679 (#625/#624/#469/#619/#622); disposition **not resolved** |
| #609 closed prematurely on a truncated acceptance read, then reopened | Preserved as process evidence in #609 |
| #717 (#670) merged with a DECISION REQUIRED, no clean PASS marker | Dispositioned as a false Crossroad (stale gate hierarchy) per #480/#677; repaired by attaching #670 under #480 |
| #476 run: local/CI doctor mismatch (released plugin vs working repo) | Recorded in #476/#574; avoidable-friction evidence |

## Governor provenance coverage and gaps

- **CANONICAL coverage: none.** The #710 primitive can canonically record any
  installed, pinned released governor (v0.22.0 or v0.23.0 alike), but it records
  one only when such a governor stamps the work. Verified directly: **no listed
  commit or PR in this report carries a `Spark-Governed-By` trailer or a `Governed
  by Spark vX.Y.Z` line** — including the #710 PR #712 itself. So nothing here is
  CANONICAL, because no governor fact was stamped, not because of any release
  window.
- **HISTORICALLY EVIDENCED:** the released-v0.22 BSD engagement only — its
  governor (v0.22.0) rests on the v0.22.0 release window **plus** the attested
  prose in #171/#679 naming it, which is per-engagement independent evidence.
- **NOT ASSESSED:** all six self-host review cycles (#706/#707/#708/#712/#716/#717)
  — no trailer, no governor line, and no independent per-work-unit evidence naming
  the governor; date-window timing alone is not treated as sufficient. Also the
  per-commit governor for the individual consumer PRs beyond the engagement-wide
  window.

## Remaining NOT ASSESSED

1. Comparable before/after efficiency measurement — does not exist (single #476
   baseline; #609 post-change comparison self-flagged as not produced as
   evidence).
2. Provider-instrumented token/cost/latency for any work unit (figures are
   approximate self-reports).
3. Three of five #480 representative workloads (small PR review, Triage on an
   existing repo, full certification/release check).
4. The released governor for the six self-host review cycles — none carries a
   `Spark-Governed-By` trailer or `Governed by Spark` line, and date-window timing
   alone is not treated as sufficient (no governor fact was stamped; this is not a
   claim that the work predates any particular release).
5. Execution model/provider/surface identity for the consumer engagement (#711,
   v0.24 — out of scope here).

## Release conclusion against #480's measurement requirement

**#480's measurement criterion is NOT YET SATISFIED.** The record shows Spark's
optimization and governance primitives were genuinely exercised in the field
(released-v0.22 consumer usage) and in self-host (review cycles, telemetry-truth
corrections, the governor primitive), with failures and boundaries preserved. But
the specific gate claim — *representative measurements show material improvement in
at least two cost/latency/token/tool dimensions without reducing correctness* —
is **not supported by comparable evidence**: there is a single baseline and no
after-measurement, three of five representative workloads are NOT ASSESSED, and
the #480 acceptance checkbox for that claim remains unchecked.

Correctness evidence is strong (green suites, doctor healthy, honest reviewer
cycles). The efficiency-improvement claim is not yet evidenced. Per #480, this
report therefore leaves that gate criterion **unsatisfied** rather than inferring
improvement from missing comparisons. Closing it requires a comparable
before/after measurement pass across the representative workloads, ideally with
provider-instrumented execution telemetry, before final human-owned v0.23 release
approval.
