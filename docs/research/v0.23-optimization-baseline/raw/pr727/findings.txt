## r1 f8c825d CHANGES REQUIRED
  1.1 - `plugins/spark/lib/execution.sh:2330-2353` — A zero-argument `spark merge-authority` invocation prints usage and returns `0`, even though `0` is documented as `ROUTINE MERGE`. Status-only callers can therefore interpret completely absent evidence as merge au
  1.2 - `plugins/spark/lib/execution.sh:2225-2250` — Duplicate known fields silently overwrite earlier values. For example, `review=fail review=pass` becomes eligible, and a named reserved boundary can be erased by a later whitespace value. This contradicts the stat
  1.3 - `docs/adr/0032-bounded-increment-merge-authority.md:88-90` — The ADR explicitly leaves the shipped `ship` instruction contradicting the newly accepted authority model. This exact HEAD therefore documents authority that its operational skill still prohibits. 
  1.4 - Governance close-out also remains unresolved: trusted validation reports issue `#726` has no release disposition. Choosing a milestone or disposition is a human project decision.
## r2 073fbd7 CHANGES REQUIRED
  2.1 - `plugins/spark/lib/execution.sh:2330-2353` — Zero-argument `spark merge-authority` still returns `0`, the same status documented for `ROUTINE MERGE`. Missing evidence must return `NOT ELIGIBLE`/`4`; only explicit `-h`/`--help` should succeed. `tests/test-mer
  2.2 - `plugins/spark/lib/execution.sh:2225-2250` — Duplicate known fields remain last-write-wins. Inputs such as `review=fail review=pass` can become eligible, while a later duplicate can erase a reserved boundary. Reject every duplicate known field and add regres
  2.3 - `docs/adr/0032-bounded-increment-merge-authority.md:88-90` — The consequences section still says `ship/SKILL.md` remains unchanged and reconciliation is deferred, but this HEAD changes that skill and adds the bounded-merge procedure. Update the ADR to descri
  2.4 - Trusted governance validation also reports that issue `#726` lacks a release disposition. Selecting a milestone or disposition remains a human project decision; do not infer or assign it automatically.
## r3 3735edd CHANGES REQUIRED
  3.1 - `plugins/spark/lib/execution.sh:2304-2325` — `parent-authorizes` is accepted solely when non-whitespace and not on the coordination denylist. Consequently, a bare reference such as `parent-authorizes="#722"` or arbitrary text can produce `ROUTINE MERGE` when
  3.2 - `plugins/spark/lib/execution.sh:2282-2301` — malformed optional boundary input does not consistently fail closed. A non-empty `surface` without `reserved-boundary`, or an explicitly empty/whitespace boundary, is ignored and can still yield `ROUTINE MERGE`. R
  3.3 - Trusted governance validation remains red: issue `#726` lacks required category and docs-impact metadata, and it is outside the v0.24 gate hierarchy. Any semantic or roadmap choices needed to resolve those findings remain human decisions.
  3.4 - ADR-0032 materially moves trunk-integration authority. Its acceptance cannot be established merely by declaring `Status: Accepted` in this PR-controlled file; durable human approval must be confirmed before close-out.
## r4 c1ed999 CHANGES REQUIRED
  4.1 - `plugins/spark/lib/execution.sh:2304-2348` — Shell globs do not enforce the documented record formats:
  4.2 - `plugins/spark/lib/execution.sh:2282-2303` — Qualified `parent-authorizes` values only have their suffix after the final `#` checked. Inputs such as `anything#still#722`, `/repo#722`, or `owner/repo/extra#722` are accepted despite documentation requiring exa
  4.3 - Trusted governance evidence still reports `#726` outside the v0.24 `#481` hierarchy. Later comments claim an approved projection, but no trusted read-back confirming that relationship is supplied; governance close-out therefore remains unresolved.
## r5 2abdd5e CHANGES REQUIRED
  5.1 - `plugins/spark/lib/execution.sh:2260-2300,2420-2450` — `xm_auth_record` validates only citation shape and never binds the record to `parent-authorizes` or to the bounded child. For example, `parent-authorizes="#722"` with `authorization-record="#999#issuecom
  5.2 - `plugins/spark/lib/execution.sh:2245-2300` — Reusing `xm_issue_ref` also accepts undocumented forms such as `sub-issue:owner/repo#724` and `owner/repo#722#issuecomment-456`, despite the CLI specifying exact forms. Enforce the documented complete grammar.
  5.3 - `tests/test-merge-authority.sh:210-247` — Existing tests cover malformed citations but not valid-yet-unrelated ones. Add fully eligible-shaped regressions for a comment/URL on another issue or repository and for an unverified/mismatched sub-issue relationshi
## r6 bc1f5ff CHANGES REQUIRED
  6.1 - `plugins/spark/lib/execution.sh:2245-2300` — `xm_auth_record` verifies only that the record points to the parent issue. For qualified parents, the bare issue URL itself is accepted, so `parent-authorizes=owner/repo#722` plus `authorization-record=https://git
  6.2 - `plugins/spark/lib/execution.sh:2420-2450` — Even comment records are not checked for author, content, or relation to this bounded child. Any unrelated comment on the parent can satisfy the gate; `child-acceptance` is also uncited free text. Binding a citati
  6.3 - `tests/test-merge-authority.sh:210-330` — Tests affirm same-parent URL/comment shapes but do not cover a valid same-parent comment unrelated to the child, or the plain parent URL as reference-only evidence.
## r7 a1f6174 CHANGES REQUIRED
  7.1 - `plugins/spark/lib/execution.sh:2380-2600` — `review=pass`, `checks=green`, `stale-head=protected`, and `scope=routine-reversible` are accepted verbatim from the caller. No PR or HEAD is resolved, and no review, check-run, or stale-head state is read from Gi
  7.2 - `plugins/spark/lib/execution.sh:2470-2600` — The bounded acceptance is never proven true. `acceptance-id` only identifies the acceptance bound by the comment; there is no evidence or mechanical check establishing ADR-0032 criterion 3 (“that bounded acceptanc
  7.3 - `plugins/spark/lib/execution.sh:2580-2590` — Child binding compares only `xm_issue_num`, discarding repository identity. A marker for `other/repo#724` can match caller child `jwogrady/spark#724` or `#724`. The command also never resolves the child or its par
  7.4 - Documentation currently promises exact-HEAD verification that the implementation does not perform (`plugins/spark/docs/reference/cli.md:600-686`, `plugins/spark/skills/ship/references/bounded-merge.md:13-35`). Tests must include forged `review/checks/stale-h
## r8 eedd339 CHANGES REQUIRED
  8.1 - `plugins/spark/lib/execution.sh:2310,2465-2490` — Check evaluation does not determine the repository’s required checks. It reads only the check-runs collection and declares green when any returned runs are terminal with `success`, `skipped`, or `neutral`. It
  8.2 - `plugins/spark/lib/execution.sh:2305-2315,2360-2465` — Trusted comment and check reads are not paginated. Parent/PR comments are capped at 100 and check-runs use the API default page, so a conflicting grant, acceptance record, review verdict, or failing chec
  8.3 - `plugins/spark/lib/execution.sh:2415-2465` — Review and acceptance conflicts fail open. Any matching PASS sets `rev=yes`; a later same-HEAD `CHANGES REQUIRED` does not invalidate it. Acceptance counts only valid `MET` records, while malformed or same-contrac
  8.4 - `plugins/spark/lib/execution.sh:2525-2605` — The advertised fail-closed decision core does not require `grant-child`: when it is absent, the comparison is skipped and otherwise affirmative facts can yield `ROUTINE MERGE`. Require and canonically validate the
## r9 6e9206e CHANGES REQUIRED
  9.1 - `plugins/spark/lib/execution.sh:2310-2320,2530-2585` — Required-check derivation reads only branch-protection `.contexts[]`. It ignores app-bound `.checks[]` and repository/organization rulesets, so a same-named check from the wrong app can satisfy the gate 
  9.2 - `plugins/spark/lib/execution.sh:2460-2530` — Review and acceptance parsing handles at most one marker per comment. For example, one authorizing comment containing a valid `MET` marker followed by a same-identity `NOT-MET` marker is parsed as one successful r
  9.3 - `plugins/spark/lib/execution.sh:2390-2460` — Grant ambiguity still counts only valid grants. A valid grant beside `spark-authorizes child=#724 acceptance=<id> extra=1` leaves `grants=1` because the malformed sibling is skipped. This contradicts the documente
## r10 cd3eafd CHANGES REQUIRED
  10.1 - `plugins/spark/lib/execution.sh` (`xm_checks`, `xm_statuses`, check evaluation): failures from either observation endpoint are converted to an empty result:
  10.2 - `plugins/spark/lib/execution.sh` (`xm_pr_files`, scope derivation): changed files are fetched with non-paginated `xm_api` despite scope depending on every changed path. A PR with more than 100 files can hide a later `.github/workflows/` or `plugins/spark/set
  10.3 - `plugins/spark/lib/execution.sh` (`xm_marker_other`): malformed markers with repeated identity fields can be incorrectly classified as concerning another PR/HEAD because only the first `pr=` and `head=` tokens are retained. For example, a malformed sibling c
  10.4 - `plugins/spark/lib/execution.sh` (`xm_derive`, native-parent handling): the derived child identity is emitted and compared as `"$parent_slug#$child_num"` rather than the PR repository’s `"$slug#$child_num"`. If native hierarchy can reference a parent in anot
## r11 549a709 CHANGES REQUIRED
  11.1 - `plugins/spark/lib/execution.sh:grant parsing in xm_derive` — For a cross-repository parent, `author_association` comes from the **parent repository’s** comment endpoint. An `OWNER`/`MEMBER`/`COLLABORATOR` there is therefore treated as authorized to grant me
  11.2 - `plugins/spark/lib/execution.sh:acceptance parsing in xm_derive` — Malformed acceptance markers are still silently ignored when required identity fields are absent or invalid. For example, beside a valid `MET`, a marker with the same child/head/contract but 
## r12 f4ebacc CHANGES REQUIRED
  12.1 - `plugins/spark/lib/execution.sh:2350-2400` (`xm_comments`, `xm_body_lines`) — Newlines are encoded as the literal two-character sequence `\n`, but existing literal `\n` text is not escaped first. An authorizing comment such as `Example:\nspark-authorizes chi
  12.2 - `plugins/spark/lib/execution.sh:2420-2475, xm_derive reviewer loop` — `xm_review_fields` validates field order and verdict but not canonical PR/SHA identity. A malformed sibling such as `pr=0727 head=<current> verdict=CHANGES REQUIRED` or `pr=727 head=deadbe
  12.3 - `plugins/spark/lib/execution.sh:parent URL parsing in xm_derive` — An invalid repository identity extracted from `parent_issue_url` silently falls back to the PR repository (`xm_repo_id "$parent_slug" || parent_slug="$slug"`). That can redirect an unreadable
## r13 d47d0e8 CHANGES REQUIRED
  13.1 - `plugins/spark/lib/execution.sh` (`xm_marker_other`, and its reviewer/acceptance call sites in `xm_derive`): malformed markers can still be dismissed using **noncanonical** identities. For example, beside a valid record:
  13.2 - PR metadata: no closing issue reference was found, despite the added contract describing a closing reference as the authoritative PR-to-work-unit linkage (`plugins/spark/docs/reference/cli.md`). Add the intended closing reference, such as `Closes #726`, or i
## r14 ee6cf2a CHANGES REQUIRED
  14.1 - `plugins/spark/lib/execution.sh` (`xm_derive`, grant and acceptance comment loops) — Same-repository authorization relies only on `author_association` being `OWNER`, `MEMBER`, or `COLLABORATOR`. Those associations do not establish write/maintain/admin permis
  14.2 - `plugins/spark/lib/execution.sh` (`xm_comments`, `xm_derive`) — The model promises durable authorization “in advance” and forbids inventing authority after a PR reaches PASS, but comment timestamps are not read or compared. A parent grant added—or an old com
## r15 34b136b CHANGES REQUIRED
  15.1 - `plugins/spark/lib/execution.sh` (`xm_derive`, scope classification near changed-file handling): release detection relies only on the PR head matching `release-please--*` or `release/*`. A release/version PR on another branch can therefore be classified `rou
  15.2 - `plugins/spark/lib/execution.sh` (`cmd_merge_authority --help`): help says a grant need only come from an `OWNER`, `MEMBER`, or `COLLABORATOR`, contradicting the implemented and documented requirement for verified `admin`/`maintain`/`write` permission. Updat
  15.3 - `plugins/spark/preferences/cli-stability.tsv:83`: `merge-authority` is recorded as introduced in v0.23, while issue #726 and this change are assigned to v0.24. Correct the compatibility metadata.
## r16 e3ced28 CHANGES REQUIRED
  16.1 - `plugins/spark/lib/execution.sh` (`xm_derive`, parent grant loop) — A grant still requires `OWNER`, `MEMBER`, or `COLLABORATOR` association in the **parent’s repository** before the author’s permission in the PR repository is checked. For a supported cross-r
  16.2 - `plugins/spark/lib/execution.sh` (`xm_derive`, reviewer loop and advance-authorization calculation) — Advance authorization is compared with the review comment’s `created_at`, although the implementation itself states that the reviewer lane upserts its marke
