# Duplicate-Truth Baseline — issue #737

Frozen worktree `/home/john/code/spark/.claude/worktrees/baseline-921c982` (HEAD 921c982). Read-only; nothing modified. Surfaces measured: `plugins/spark/bin/spark` (8881 L), `plugins/spark/lib/{execution,planning,repository}.sh`, `plugins/spark/hooks/guard-bash.sh`, `plugins/spark/scripts/hooks/{pre-commit,commit-msg}`, `.github/scripts/**` (9 scripts), `.github/workflows/*.yml` (inline bash counts as shipped script). Docs, skills prose, and `tests/` excluded from counts.

Structural finding up front: **there is no shared GitHub-transport helper anywhere in the repo.** Every consumer opens its own `gh` call and its own parse. That is the root multiplier behind concepts 1, 4, 5, 10, 11, 12, 13.

| # | Concept | Operative defs | Likely canonical primitive | Other surfaces (file:line) | Evidence | Drift risk |
|---|---------|---------------|---------------------------|---------------------------|----------|-----------|
| 1 | Repository identity | **8** | `plugins/spark/lib/repository.sh:35 repo_locator_normalize` | `plugins/spark/bin/spark:6500` (hub_locator_valid + host parser 6528–6600); `plugins/spark/bin/spark:3407` (di_repo_nwo); `plugins/spark/bin/spark:3144`; `plugins/spark/bin/spark:5398`; `plugins/spark/hooks/guard-bash.sh:420-423` (gh `--repo` shorthand → synthesises `github.com/`); owner/name splits `plugins/spark/bin/spark:3752,3800,4131,5400`; `.github/workflows/openai-review.yml:258`; `.github/scripts/gate-runner.sh:13` + `.github/scripts/release-notes-runner.sh:475` (`GITHUB_REPOSITORY`); `.github/scripts/docs-truth.sh:108-109` (`{owner}/{repo}` literal fallback) | Three verbatim `gh repo view --json nameWithOwner --jq .nameWithOwner` copies in one file; two unrelated locator grammars (URL/ssh normaliser vs. hub validator) | **HIGH** — CI lane uses env, CLI uses `gh`, guard uses git remote; a host/enterprise change touches 8 places |
| 2 | Issue / work-unit identity | **7** | `plugins/spark/bin/spark:1933 issue_refs` (documented as the one `#N` syntax) | `plugins/spark/bin/spark:3662` (branch `^[a-z]+/<n>-`); `plugins/spark/bin/spark:8327` + `:8332-8333` (closing-ref awk + heuristics); `.github/scripts/openai-review/lib.sh:30-32` (orl_closing_issues); `plugins/spark/bin/spark:3496 di_linked_prs` (GraphQL `closedByPullRequestsReferences`); `.github/scripts/ledger-truth-check.sh:98,185`; `.github/scripts/claude-lane/resolve.sh:22,45` (comment issue-number == PR number) | `issue_refs` claims to own the `#N` rule, yet three other regexes for the same fact ship | **HIGH** — the closing-ref vocabulary exists twice with different tolerances (see pair 7) |
| 3 | Canonical identifiers (SHA + trunk) | **12** (4 SHA, 8 trunk) | SHA: `.github/scripts/claude-lane/lib.sh:39 cl_check_stale_head` (+ validation at `:17-18`); trunk: `plugins/spark/bin/spark:2039 repo_trunk` | SHA: `plugins/spark/lib/execution.sh:196 tm_binding_status`; `.github/workflows/openai-review.yml:128` and `:347` (inline `live_head` compares); `.github/scripts/claude-lane/publish.sh:45,62,90`. Trunk: `plugins/spark/bin/spark:3297 di_trunk`; `:810` (`gh api … default_branch`); `:2108`, `:4882` (literal `master\|main`); `plugins/spark/scripts/hooks/pre-commit:11`; `plugins/spark/hooks/guard-bash.sh:149`; `.github/scripts/claude-lane/resolve.sh:45` (`base.repo.default_branch`); `.github/scripts/{gate-runner.sh:14,release-notes-runner.sh:476}` (`release-please--branches--master` literal) | Only `claude-lane` validates SHA shape (hex, 40/64); everyone else string-compares. Trunk is derived locally, remotely, and hard-coded in the same repo | **HIGH** — renaming the default branch breaks two literal `…--branches--master` strings and two `master\|main` cases silently |
| 4 | GitHub transport / parsing | **~14** (10 unshared `gh` wrappers + 4 non-jq fallback parsers) | none — no `gh_api()` exists | Wrappers: `plugins/spark/bin/spark:3407,3496,3578,3904,4126,4447,5396,8296`; `plugins/spark/lib/execution.sh:206,1787`. Strategies: `gh --jq` (58 sites/9 files), Bash-side `\| jq -r` (`claude-lane/resolve.sh` ×5, `gate-runner.sh` ×4, `milestone-gate.sh` ×2, `release-notes-runner.sh` ×3, `openai-review.yml` ×7, `bin/spark` ×7), `python3` fallback (`bin/spark:120,644,5083,6418,6717`; `milestone-gate.sh:52,73`), regex fallback (`bin/spark:325 read_flat_json` awk, `:649` grep, `:3468` sed, `:7548 ruleset_required_checks` grep, `:7585` grep over a `.sh`) | Four different answers to "read JSON without jq"; `ruleset_required_checks` greps a JSON file by design | **HIGH** — a `gh` output-shape change has 58 independent landing sites |
| 5 | Pagination / list collection | **6 strategies / ~20 sites** | `plugins/spark/bin/spark:3506-3567` (di_linked_prs manual cursor loop, the only one with fail-closed cursor handling) | Bounded-page + `hasNextPage`→`unread`: `bin/spark:4126-4181`, `:5396-5445` (two copies of the same idea); `gh api --paginate`: `bin/spark:3579,3905,4474`, `lib/planning.sh:246,277,298,328`, `openai-review.yml:142,204,296,328,512,517`; GraphQL `--paginate`: `openai-review.yml:260`; `--limit` bounds: `bin/spark:8301` (limit-reached ⇒ `unassessed`), `bin/spark:8518` (`--limit 200`, no completeness check), `gate-runner.sh:28` (500), `docs-truth.sh:144` (200), `docs-truth.yml:52` (50) | Three incompatible truncation semantics coexist: fail-closed, `unread`-row, and silent | **HIGH** — silent-truncation sites are the historical bug class (#530) and 5 still exist |
| 6 | Authority / permission | **7** | `.github/scripts/claude-lane/lib.sh:12 cl_resolve_publication` | `.github/scripts/claude-lane/lib.sh:29 cl_check_identity` (overlapping second copy); `.github/workflows/claude.yml:40-42` (`author_association` ∈ OWNER/MEMBER/COLLABORATOR); `.github/scripts/openai-review/lib.sh:15-16` (`github-actions[bot]`/`github-actions`, enforced in `:63,:79,:99`); `plugins/spark/lib/repository.sh:98 repo_authorize`; `plugins/spark/hooks/guard-bash.sh:394 guard_is_read_only`; `plugins/spark/bin/spark:315 permission_baseline` → 2 JSON allowlists (`settings/permission-baseline*.json`, 24 vs 16 `Bash(` entries); `plugins/spark/scripts/hooks/commit-msg:69` (`spark.governorBin` pin) | Trust is expressed four unrelated ways: association literal, bot-login literal, locator equality, command-shape allowlist | **MED** — the lanes are genuinely different trust domains, but `cl_resolve_publication`/`cl_check_identity` duplicate within one file |
| 6b | Collaborator permission API (`repos/…/collaborators/…/permission`, admin/maintain/write set) | **0** | — | — | No such call anywhere in shipped code | n/a (absent, not drifted) |
| 7 | Reviewer verdict representation | **6 marker surfaces + 3 verdict vocabularies** | `.github/scripts/openai-review/lib.sh:36 orl_marker` (sole emitter) | Parsers: `lib.sh:63 orl_has_trusted_claim`, `:79 orl_has_final_marker`, `:99 orl_has_consumed` (three separate case-glob parsers of the same string), plus inline glob `.github/workflows/openai-review.yml:543` and jq needles `:167,:530,:560`. Vocabularies: `lib.sh:19 orl_normalize_verdict`; `plugins/spark/lib/execution.sh:51 TELEMETRY_VERDICTS` (adds `FAIL`); ad-hoc verdict strings `plugins/spark/bin/spark:3599 di_report`, `lib/planning.sh:679,752,770,782`, `lib/execution.sh:2138 xr_stop_check`, `bin/spark:4858` (disposition vocabulary `KEEP\|REWRITE-COLLAPSE\|DROP-ARCHIVE\|DECISION-REQUIRED`) | The `spark-openai-review` marker literal appears **only** in the reviewer lane; no `plugins/**` code parses it (the #585 consumer does not exist yet) | **MED** for the marker (one file), **HIGH** for the verdict vocabulary (3 lists, one with an extra token) |
| 8 | Acceptance representation | **0 operative parsers** for acceptance criteria / checkboxes; **2** for the nearest shipped proxy (docs-impact disposition) | — | Proxy: `plugins/spark/bin/spark:3647` (docs-impact family read from the governance model) vs `.github/scripts/docs-truth.sh:145,161` (hard-coded `docs-impact:` prefix and `docs-impact:none` literal); issue-form label parity `bin/spark:1515-1530`; `.github/scripts/alpha-intake-check.sh:26-35` (an `id: privacy` checkbox block) | "acceptance criteria" exists only as prose in `skills/**` and `docs/**`; nothing in bash parses `- [ ]` acceptance lists | **LOW** for acceptance itself (nothing to drift); **MED** for docs-impact (data-driven vs literal) |
| 9 | Exact-HEAD / staleness | **4** (head-staleness) + 3 unrelated senses of "stale" | `.github/scripts/claude-lane/lib.sh:39 cl_check_stale_head` | `.github/workflows/openai-review.yml:128`, `:347` (two inline live-head re-reads); `plugins/spark/lib/execution.sh:196 tm_binding_status` + `:206 tm_live_head`; `plugins/spark/lib/execution.sh:1869-1881` (`CI_OBS_VERDICT="stale"` for a moved head). Other senses: `bin/spark:2015-2030` (orientation stale), `lib/execution.sh:1051-1110` (evidence stale), `bin/spark:1198` (cli-stability stale) | Four independent "is this the same HEAD" implementations; `headRefOid` vs `.head.sha` used interchangeably | **MED** — semantics agree today, but "stale" names four different facts in one codebase |
| 10 | Parent / sub-issue state | **5** | `plugins/spark/bin/spark:5396 milestone_snapshot` (GraphQL `parent` + `subIssues`) | `plugins/spark/bin/spark:4126 gov_gate_capture` (`parent{number}` at `:4145,:4157`); `plugins/spark/lib/planning.sh:277` and `:328` (two REST `sub_issues` loops in one function); `plugins/spark/bin/spark:5521-5544` (awk child walk); `bin/spark:8590,8665` (container-vs-leaf rule in cmd_next) | GraphQL `subIssues` and REST `/issues/N/sub_issues` are both live, in different verbs | **HIGH** — two API shapes for one graph; the REST readers paginate, the GraphQL ones bound at `first:50` |
| 11 | True dependency state | **7** | `plugins/spark/bin/spark:4535` (blocked_by with same-repo edge filter — the only correct one) | `plugins/spark/lib/planning.sh:298` (`--paginate`, `.[].number`); `plugins/spark/bin/spark:8709` (open-count, no pagination); `plugins/spark/bin/spark:4477` (`issue_dependencies_summary.blocked_by` used as a pre-filter); `bin/spark:4350 gov_cycle_rows` (cycle detection over the edges). Closing-refs: `bin/spark:3496 di_linked_prs` (GraphQL) vs `openai-review/lib.sh:30` (text) vs `bin/spark:8327` (text) | Only one of three `blocked_by` readers filters cross-repo blockers; only one paginates | **HIGH** — a cross-repo blocker or a >30-blocker issue yields three different answers |
| 12 | Milestone / release placement | **~13** | `.github/scripts/milestone-gate.sh:87` (version `X.Y` → milestone title `^vX.Y`) | Milestone selection: `.github/workflows/docs-truth.yml:77` (first open milestone by number); `.github/scripts/docs-truth.sh:120` (`v[0-9]+\.[0-9]+` token vs ROADMAP); `plugins/spark/bin/spark:5632 current_milestone` (open milestone with open leaf work); `lib/planning.sh:246` (existence by title). Release-PR discovery: `gate-runner.sh:14/16` and `release-notes-runner.sh:476/478` (literal `release-please--branches--master`) vs `docs-truth.yml:52` (`startswith("release-please--")`) vs `bin/spark:3457 di_release_branch_prefix` (derived from `release-please-config.json`). Version reads: `bin/spark:638 cmd_version` (jq→python3→grep) vs `milestone-gate.sh:50-52` (jq→python3) vs `gate-runner.sh:26` (contents API + base64) vs `release-notes-runner.sh:112 notes_last_tag` (git tag). Component vocab: `release-notes-runner.sh:41,98`, `NOTES_COMPONENTS`, `release-please-config.json` packages, reconciled by `bin/spark:7566 check_release_component_parity` | Four different "which milestone is the release", four "what version is this", four "where is the release PR" | **HIGH** — highest fan-out concept in the repo; a branch-prefix or milestone-naming change hits 3 lanes independently |
| 13 | Required checks | **8** (and **4 disagreeing copies of the check-name set**) | `.github/scripts/openai-review/lib.sh:271 orl_checks_terminal` / `:294 orl_checks_passed` | Name sets: `.github/workflows/openai-review.yml:199` **and** `:320` (`docs-truth doctor gate tests`, twice), `.github/scripts/gate-runner.sh:37` (`doctor`, `tests`, `docs-truth` — no `gate`), `.github/spark-trunk-ruleset.json:29-30` (`doctor`, `tests`), `plugins/spark/settings/github-ruleset-trunk.json:29` (`validate`). Readers: `plugins/spark/bin/spark:818-830` (rules/branches + `branches/*/protected`), `:7489 remote_enforcement_verdict`, `:7548 ruleset_required_checks` (grep over JSON), `plugins/spark/lib/execution.sh:1787` (`statusCheckRollup` — a fourth checks source) | Four literal sets, none derived from another; shipped template requires `validate` while this repo requires `doctor`+`tests` | **HIGH** — adding a required check must be edited in ≥4 files or the reviewer gate silently under-waits |
| 14 | Governance / reference truth in code | **6 clusters** | none — several exist only as reconcilers | Version literal: `plugins/spark/.claude-plugin/plugin.json:4` + `.release-please-manifest.json:2` (`0.22.0`), ×4 components. Label vocabulary: `preferences/defaults.json` `issue.taxonomy` vs `preferences/governance-models/*` category family — reconciled by `plugins/spark/bin/spark:1052-1073`. CLI vocabulary: `preferences/cli-stability.tsv` vs the in-binary `VERBS` table vs `docs/reference/stability.md` — reconciled by `bin/spark:1167-1223`. Crossroad vocabulary: `plugins/spark/lib/execution.sh:2131-2133` (`XR_BOUNDARY_KINDS`/`XR_NON_BOUNDARY_KINDS`) vs skills prose. Verdict vocabularies: see row 7. Release components: see row 12 | Doctor ships two checks whose entire purpose is to reconcile duplicated vocabularies — duplication is acknowledged and policed, not removed | **MED** — policed duplicates are lower risk than unpoliced ones, but the policing is itself a third copy |
| 14b | "Six #677 conditions" hard-coded in code | **UNKNOWN / not found** | — | Only `tests/test-crossroad.sh:59` and `tests/test-benchmark-vocab.sh:50` mention #677; no six-condition list exists in `plugins/**` or `.github/**` | grep for `677` in shipped code returns no operative list | n/a |
| 14c | Hard-coded released *governor* version | **0** | — | `spark.governorBin` (`bin/spark:6776`, `scripts/hooks/commit-msg:69`) pins a **path**, not a version | No version literal is compared anywhere | n/a |

## Ten most concrete duplicate pairs

1. **`.github/workflows/openai-review.yml:199`** ⇄ **`.github/workflows/openai-review.yml:320`** — the identical literal `required="docs-truth doctor gate tests"` written twice in one file (terminality gate vs. pre-call greenness gate); nothing binds them, so one can be edited alone.
2. **`.github/workflows/openai-review.yml:199`** ⇄ **`.github/scripts/gate-runner.sh:37`** — same "required checks" concept, different membership and form: a shell word list including `gate` vs. a jq `select(.name=="doctor" or .name=="tests" or .name=="docs-truth")` that omits `gate` entirely.
3. **`.github/spark-trunk-ruleset.json:29-30`** ⇄ **`plugins/spark/settings/github-ruleset-trunk.json:29`** — the repo's own policy-as-data requires `doctor`+`tests`; the shipped template for every downstream repo requires `validate`. `plugins/spark/bin/spark:7548` greps whichever it finds first, so the two never meet.
4. **`plugins/spark/lib/repository.sh:35 repo_locator_normalize`** ⇄ **`plugins/spark/bin/spark:6500 hub_locator_valid`** (host/path extraction `:6528-6600`) — two independently written repository-locator grammars: one normalises ssh/https/`.git` to `host/owner/name`, the other validates shorthand/URL/scp forms with its own bracket-IPv6 and userinfo handling. Neither calls the other.
5. **`plugins/spark/bin/spark:3407`** ⇄ **`plugins/spark/bin/spark:3144`** ⇄ **`plugins/spark/bin/spark:5398`** — three verbatim copies of `gh repo view --json nameWithOwner --jq .nameWithOwner` in one file; `:3407` is the named helper `di_repo_nwo`, the other two inline it, and `:5398` returns non-zero on failure while `:3144` swallows it with `|| true`.
6. **`plugins/spark/bin/spark:2039 repo_trunk`** ⇄ **`plugins/spark/bin/spark:3297 di_trunk`** — same algorithm (`origin/HEAD`, else probe master then main), different outputs: `repo_trunk` emits TSV `trunk<TAB>name` + `source`, `di_trunk` emits a bare `origin/<name>` ref; `repo_trunk` probes local branches, `di_trunk` probes remote-tracking refs.
7. **`plugins/spark/bin/spark:8327`** ⇄ **`.github/scripts/openai-review/lib.sh:31`** — GitHub's closing-reference vocabulary twice: awk `(close[sd]?|fix(e[sd])?|resolve[sd]?)[: ]*#N([^0-9]|$)` (tolerates `Closes: #12`, matches one known issue, case-folded via `tolower`) vs. `grep -oiE '(close[sd]?|fix(e[sd])?|resolve[sd]?) #[0-9]+'` (requires exactly one space, extracts all issues, `sort -un`).
8. **`plugins/spark/bin/spark:4535`** ⇄ **`plugins/spark/bin/spark:8709`** ⇄ **`plugins/spark/lib/planning.sh:298`** — three readers of `repos/{owner}/{repo}/issues/N/dependencies/blocked_by`: `:4535` filters to same-repo open blockers and builds edges, `:8709` returns only a count of open blockers with no pagination, `planning.sh:298` paginates and returns bare numbers. Only one paginates; only one guards cross-repo blockers.
9. **`plugins/spark/lib/planning.sh:277`** ⇄ **`plugins/spark/lib/planning.sh:328`** — the same `gh api …/sub_issues --paginate --jq '.[].number'` fetch written twice inside `plan_relation_rows`, once for the hierarchy check and once for the order check, with different failure rows (`hierarchy\t?` vs `order\t?`) and no caching between them.
10. **`.github/scripts/gate-runner.sh:14-23,58-70`** ⇄ **`.github/scripts/release-notes-runner.sh:476-485,518-529`** — a wholesale duplicated pipeline: same literal release branch, same `gh pr list --json number,headRefOid`, same `jq -r '.number'`/`.headRefOid` extraction, same `POST repos/$repo/statuses/$head_sha`, same marker-comment upsert (`<!-- milestone-gate -->` vs `<!-- release-notes-check -->`) via the same three `gh api` calls. Only differences: the marker string, the `body` field in the PR JSON, and error handling on an unresolvable head.

Honourable mentions (not in the top ten): `.github/scripts/claude-lane/lib.sh:12 cl_resolve_publication` ⇄ `:29 cl_check_identity` (overlapping fork-head / head-is-default-branch checks, one at resolve time and one at publish time, with different refusal token vocabularies); `.github/workflows/openai-review.yml:128` ⇄ `:347` (two inline `gh api "repos/$REPO/pulls/$PR" --jq '.head.sha'` supersession re-reads); `plugins/spark/bin/spark:644` ⇄ `.github/scripts/milestone-gate.sh:52` (two hand-written python3 JSON-version readers).
